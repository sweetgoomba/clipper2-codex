# 2026-06-08

2026-06-08 Clipper2 web/admin/API dev deployment follow-up.

- User asked to continue Clipper2 web/admin/API dev deployment from
  `/Users/jina/project/adlight`.
- Read first:
  - `.codex/handoff/NEXT.md`
  - `.codex/records/sessions/2026/06/05.md`
  - `.codex/design/CLIPPER2_WEB_DB_SPLIT_AND_DEV_DEPLOYMENT.md`
  - `.codex/design/CLIPPER2_INFRA_TECHNICAL_GUIDE.md`
  - `clipper_infra/README.md`
  - `clipper_infra/runbooks/deploy-db.md`
  - `clipper_infra/apps/compose.yml`
  - `clipper_infra/apps/compose.dev.yml`
  - `clipper_infra/env/stack.dev.env.example`
- User clarified that the temporary m2-stage nginx dev test containers had
  already been removed.
- Public dev domains currently return `HTTP/2 502`, which matches NPM being
  alive while the real upstream services are not deployed yet:
  - `https://dev.clipperstudio.ai`
  - `https://dev-admin.clipperstudio.ai`
  - `https://dev-api.clipperstudio.ai/healthz`
- Local machine could not reach the LAN hosts directly during this session:
  - `192.168.0.23` SSH timed out.
  - `192.168.0.2`, `192.168.0.7`, and `192.168.0.23` ping/TCP checks timed out.
  - User normally accesses m2-stage via Google Remote Desktop, so the next
    deployment session should be opened directly on m2-stage.

## Deployment Strategy Decision

- Chosen dev deployment strategy: server-side build on m2-stage.
- Registry/GHCR push-pull is not the current dev path.
- Reason:
  - The team already uses server-side builds in dohit.
  - This deployment goal is first dev wiring, not full CI/CD hardening.
  - Registry push requires additional image publishing, auth, tag, and approval
    decisions.
- Recommended m2-stage repo layout:

```text
/Users/metabuzz/Desktop/project/clipper2/
  clipper_infra/
  clipper_web_client/
  clipper_web_admin/
  clipper_web_api/
```

- `clipper_infra/runbooks/deploy-dev.md` was updated to document this sibling
  repo layout and the `git pull -> docker build -> docker compose up -d
  --force-recreate` flow.
- `clipper_infra/env/stack.dev.env.example` was updated to use local image
  names:
  - `clipper-web-client:dev`
  - `clipper-web-admin:dev`
  - `clipper-web-api:dev`

## Web/Admin/API Scaffold Correction

- User asked whether the three web repos are already using the intended
  framework stack.
- Confirmed they were not: prior state was static Nginx web/admin plus a
  dependency-free Node HTTP API.
- Converted local working trees to the intended stack:
  - `clipper_web_client`: Angular 19 scaffold with a simple placeholder page.
  - `clipper_web_admin`: Angular 19 scaffold with a simple placeholder page.
  - `clipper_web_api`: NestJS scaffold preserving current health/info behavior.
- Installed/locked versions:
  - `clipper_web_client`: `@angular/core@19.2.25`,
    `@angular/cli@19.2.27`, `@angular-devkit/build-angular@19.2.27`.
  - `clipper_web_admin`: `@angular/core@19.2.25`,
    `@angular/cli@19.2.27`, `@angular-devkit/build-angular@19.2.27`.
  - `clipper_web_api`: `@nestjs/common@11.1.24`,
    `@nestjs/core@11.1.24`, `@nestjs/platform-express@11.1.24`.
- Current web/admin behavior:
  - URL root displays only a placeholder.
  - No login/signup/download UI is implemented in these placeholders.
  - This is intentional because a teammate is developing the real client-facing
    pages separately.
- Current API behavior:
  - `GET /healthz`
  - `GET /v1/health`
  - `GET /v1/info`
  - `GET /v1/releases/latest` returns `501 release_metadata_not_implemented`.
  - It still does not connect to PostgreSQL; it only reports whether DB URL env
    values are configured.

## Dohit Reference Check

- User asked to inspect `/Users/jina/project/dohit`.
- Actual local repo paths are hyphenated:
  - `/Users/jina/project/dohit/dohit-angular`
  - `/Users/jina/project/dohit/dohit-nestjs`
  - `/Users/jina/project/dohit/dohit-infra`
- `dohit-angular` was clean and was fast-forwarded to
  `b0433a2 fix: 쿼리 데이터가 없을 경우 mock 데이터 보여지도록 수정`.
- `dohit-nestjs` was fetched, but not pulled because local `.env.local` is
  modified and upstream `origin/stage` also changes `.env.local`.
- `dohit-infra` was fetched; local `.gitignore` is modified.
- Dohit deployment pattern:
  - server-side build from infra compose.
  - infra compose uses build contexts `./dohit-angular` and `./dohit-nestjs`.
  - app source directories are ignored inside the infra working tree on server.
  - Angular image builds with Node and then serves static files through Nginx.
  - NestJS image runs Node/NestJS directly with no Nginx.
  - NPM routes to host-published ports.
- Dohit has tracked env/secret values. Values were not repeated in the session
  notes; operationally they should be treated as exposed and rotated.
- Clipper improvement over dohit:
  - Keep app repos as siblings of `clipper_infra`, not ignored subdirectories
    inside infra.
  - Keep secrets out of Git.
  - Keep DB ports LAN-only and host-restricted.

## Verification

- `clipper_web_client npm test`: passed.
- `clipper_web_admin npm test`: passed.
- `clipper_web_api npm test`: passed (`2 passed`).
- Docker builds passed:
  - `docker build -t clipper-web-client:dev .`
  - `docker build -t clipper-web-admin:dev .`
  - `docker build -t clipper-web-api:dev .`
- `clipper_infra/apps docker compose --env-file ../env/stack.dev.env.example
  -f compose.yml -f compose.dev.yml config --quiet`: passed.
- Local Docker smoke passed:
  - web client `/healthz`
  - web admin `/healthz`
  - API `/v1/health`
  - web/admin placeholder HTML served through Nginx images.
- Smoke containers were stopped after verification.
- `git diff --check` passed for:
  - `clipper_web_client`
  - `clipper_web_admin`
  - `clipper_web_api`
  - `clipper_infra`

## Current Local Uncommitted Changes

These changes exist only in the local working trees until committed and pushed:

- `clipper_web_client`
  - Angular 19 scaffold and placeholder conversion.
  - Dockerfile changed to Angular build + Nginx runtime.
  - package lock added.
- `clipper_web_admin`
  - Angular 19 scaffold and placeholder conversion.
  - Dockerfile changed to Angular build + Nginx runtime.
  - package lock added.
- `clipper_web_api`
  - NestJS scaffold conversion.
  - package lock added.
  - health/info/release placeholder tests updated.
- `clipper_infra`
  - dev env example now uses local image names.
  - dev deploy runbook now documents `/Users/metabuzz/Desktop/project/clipper2`
    sibling repo layout and server-side build flow.
- `.codex`
  - this session note plus handoff/design updates.

Before m2-stage can build and deploy the new Angular/NestJS scaffolds, these
repo changes must be committed and pushed, or otherwise copied to m2-stage.

## Still Not Done

- The new local changes have not been committed or pushed yet.
- m2-stage real `env/stack.dev.env` still needs to be created.
- Exposed dev DB passwords should be rotated before app env wiring.
- Real web/admin/API dev services are not deployed on m2-stage yet.
- DB migrations/tables/seeds do not exist yet.
- Social login/deep-link design is not finalized.
- Payment module integration is not finalized. Current payment direction is
  manual bank transfer request/review.
