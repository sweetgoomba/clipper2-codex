# 2026-06-08

2026-06-08 Clipper2 web/admin/API dev deployment follow-up.

- User asked to continue Clipper2 web/admin/API dev deployment from
  `/Users/jina/project/adlight`.
- Read first:
  - `.codex/handoff/NEXT.md`
  - `.codex/records/sessions/2026/06/05.md`
  - `.codex/design/CLIPPER2_WEB_DB_SPLIT_AND_DEV_DEPLOYMENT.md`
  - `.codex/design/CLIPPER2_INFRA_TECHNICAL_GUIDE.md`
  - `clipper_infra/README.md`
  - `clipper_infra/runbooks/deploy-db.md`
  - `clipper_infra/apps/compose.yml`
  - `clipper_infra/apps/compose.dev.yml`
  - `clipper_infra/env/stack.dev.env.example`
- User clarified that the temporary m2-stage nginx dev test containers had
  already been removed.
- At the start of the local session, public dev domains returned `HTTP/2 502`,
  which matched NPM being alive while the real upstream services were not
  deployed yet:
  - `https://dev.clipperstudio.ai`
  - `https://dev-admin.clipperstudio.ai`
  - `https://dev-api.clipperstudio.ai/healthz`
- Local machine could not reach the LAN hosts directly during this session:
  - `192.168.0.23` SSH timed out.
  - `192.168.0.2`, `192.168.0.7`, and `192.168.0.23` ping/TCP checks timed out.
  - User normally accesses m2-stage via Google Remote Desktop, so the next
    deployment session should be opened directly on m2-stage.

## Deployment Strategy Decision

- Chosen dev deployment strategy: server-side build on m2-stage.
- Registry/GHCR push-pull is not the current dev path.
- Reason:
  - The team already uses server-side builds in dohit.
  - This deployment goal is first dev wiring, not full CI/CD hardening.
  - Registry push requires additional image publishing, auth, tag, and approval
    decisions.
- Recommended m2-stage repo layout:

```text
/Users/metabuzz/Desktop/project/clipper2/
  clipper_infra/
  clipper_web_client/
  clipper_web_admin/
  clipper_web_api/
```

- `clipper_infra/runbooks/deploy-dev.md` was updated to document this sibling
  repo layout and the `git pull -> docker build -> docker compose up -d
  --force-recreate` flow.
- `clipper_infra/env/stack.dev.env.example` was updated to use local image
  names:
  - `clipper-web-client:dev`
  - `clipper-web-admin:dev`
  - `clipper-web-api:dev`

## Web/Admin/API Scaffold Correction

- User asked whether the three web repos are already using the intended
  framework stack.
- Confirmed they were not: prior state was static Nginx web/admin plus a
  dependency-free Node HTTP API.
- Converted local working trees to the intended stack:
  - `clipper_web_client`: Angular 19 scaffold with a simple placeholder page.
  - `clipper_web_admin`: Angular 19 scaffold with a simple placeholder page.
  - `clipper_web_api`: NestJS scaffold preserving current health/info behavior.
- Installed/locked versions:
  - `clipper_web_client`: `@angular/core@19.2.25`,
    `@angular/cli@19.2.27`, `@angular-devkit/build-angular@19.2.27`.
  - `clipper_web_admin`: `@angular/core@19.2.25`,
    `@angular/cli@19.2.27`, `@angular-devkit/build-angular@19.2.27`.
  - `clipper_web_api`: `@nestjs/common@11.1.24`,
    `@nestjs/core@11.1.24`, `@nestjs/platform-express@11.1.24`.
- Current web/admin behavior:
  - URL root displays only a placeholder.
  - No login/signup/download UI is implemented in these placeholders.
  - This is intentional because a teammate is developing the real client-facing
    pages separately.
- Initial API behavior at that point:
  - `GET /healthz`
  - `GET /v1/health`
  - `GET /v1/info`
  - `GET /v1/releases/latest` returns `501 release_metadata_not_implemented`.
  - It did not yet connect to PostgreSQL; it only reported whether DB URL env
    values were configured. This was changed later by `8386865`.

## Dohit Reference Check

- User asked to inspect `/Users/jina/project/dohit`.
- Actual local repo paths are hyphenated:
  - `/Users/jina/project/dohit/dohit-angular`
  - `/Users/jina/project/dohit/dohit-nestjs`
  - `/Users/jina/project/dohit/dohit-infra`
- `dohit-angular` was clean and was fast-forwarded to
  `b0433a2 fix: 쿼리 데이터가 없을 경우 mock 데이터 보여지도록 수정`.
- `dohit-nestjs` was fetched, but not pulled because local `.env.local` is
  modified and upstream `origin/stage` also changes `.env.local`.
- `dohit-infra` was fetched; local `.gitignore` is modified.
- Dohit deployment pattern:
  - server-side build from infra compose.
  - infra compose uses build contexts `./dohit-angular` and `./dohit-nestjs`.
  - app source directories are ignored inside the infra working tree on server.
  - Angular image builds with Node and then serves static files through Nginx.
  - NestJS image runs Node/NestJS directly with no Nginx.
  - NPM routes to host-published ports.
- Dohit has tracked env/secret values. Values were not repeated in the session
  notes; do not repeat them in chat/logs.
- Clipper improvement over dohit:
  - Keep app repos as siblings of `clipper_infra`, not ignored subdirectories
    inside infra.
  - Keep secrets out of Git.
  - Keep DB ports LAN-only and host-restricted.

## Verification

- `clipper_web_client npm test`: passed.
- `clipper_web_admin npm test`: passed.
- `clipper_web_api npm test`: passed (`2 passed`).
- Docker builds passed:
  - `docker build -t clipper-web-client:dev .`
  - `docker build -t clipper-web-admin:dev .`
  - `docker build -t clipper-web-api:dev .`
- `clipper_infra/apps docker compose --env-file ../env/stack.dev.env.example
  -f compose.yml -f compose.dev.yml config --quiet`: passed.
- Local Docker smoke passed:
  - web client `/healthz`
  - web admin `/healthz`
  - API `/v1/health`
  - web/admin placeholder HTML served through Nginx images.
- Smoke containers were stopped after verification.
- `git diff --check` passed for:
  - `clipper_web_client`
  - `clipper_web_admin`
  - `clipper_web_api`
  - `clipper_infra`

## Committed And Pushed Changes

These changes were committed and pushed after the session notes were first
written:

- `clipper_web_client`
  - commit `6fa1ffc feat: scaffold Angular dev client`
  - Angular 19 scaffold and placeholder conversion.
  - Dockerfile changed to Angular build + Nginx runtime.
  - package lock added.
- `clipper_web_admin`
  - commit `ffba705 feat: scaffold Angular dev admin`
  - Angular 19 scaffold and placeholder conversion.
  - Dockerfile changed to Angular build + Nginx runtime.
  - package lock added.
- `clipper_web_api`
  - commit `d641942 feat: scaffold NestJS web API`
  - NestJS scaffold conversion.
  - package lock added.
  - health/info/release placeholder tests updated.
- `clipper_infra`
  - commit `decdfa0 docs: document Clipper dev server-side deployment`
  - dev env example now uses local image names.
  - dev deploy runbook now documents `/Users/metabuzz/Desktop/project/clipper2`
    sibling repo layout and server-side build flow.
- `.codex`
  - commit `f4207ad docs: hand off Clipper2 dev deploy session`
  - this session note plus handoff/design updates.

User reported that m2-stage currently has only a stale `clipper_infra` under
`/Users/metabuzz/Desktop/project/clipper2`. Pull `clipper_infra` and clone
`clipper_web_client`, `clipper_web_admin`, and `clipper_web_api` there before
server-side build.

## M2-stage Dev App Deployment Completed

Later on 2026-06-08, the user opened a Codex session directly on m2-stage and
completed the real dev web/admin/API deployment.

Server workspace:

```text
/Users/metabuzz/Desktop/project/clipper2/
  clipper_infra/
  clipper_web_client/
  clipper_web_admin/
  clipper_web_api/
```

Repo state used on m2-stage:

```text
clipper_infra       feature/infra-initial-setup @ decdfa0
clipper_web_client  main @ e2102db fix: include Angular zone polyfill
clipper_web_admin   main @ e21cabe fix: include Angular zone polyfill
clipper_web_api     main @ d641942 feat: scaffold NestJS web API
```

The local machine fetched GitHub afterward and confirmed:

```text
clipper_web_client origin/main -> e2102db fix: include Angular zone polyfill
clipper_web_admin  origin/main -> e21cabe fix: include Angular zone polyfill
clipper_web_api    origin/main -> d641942 feat: scaffold NestJS web API
clipper_infra      origin/feature/infra-initial-setup -> decdfa0 docs: document Clipper dev server-side deployment
```

Deployment details:

- `clipper_infra` was updated with `git fetch` and `git pull --ff-only`.
- App repos were cloned as siblings of `clipper_infra`.
- Deployment used server-side Docker builds on m2-stage, not GHCR/registry
  push-pull.
- `clipper_infra/env/stack.dev.env` was created from
  `stack.dev.env.example`. The user filled the three DB passwords manually.
- The session verified no `CHANGE_ME` placeholders remained before deploying.
- Secret values were not printed in chat/logs.
- Dev DB TCP checks from m2-stage succeeded:
  - `192.168.0.7:55203`
  - `192.168.0.7:55213`
  - `192.168.0.7:55223`
- Docker images were built on m2-stage:
  - `clipper-web-client:dev`
  - `clipper-web-admin:dev`
  - `clipper-web-api:dev`
- Compose config passed with:
  - `docker compose --env-file ../env/stack.dev.env -f compose.yml -f compose.dev.yml config --quiet`
- Real dev services were deployed with:
  - `docker compose --env-file ../env/stack.dev.env -f compose.yml -f compose.dev.yml up -d --force-recreate`

Final deployed containers:

```text
clipper-web-client-dev healthy 192.168.0.23:42203->80/tcp
clipper-web-admin-dev  healthy 192.168.0.23:42303->80/tcp
clipper-web-api-dev    healthy 192.168.0.23:43203->43203/tcp
```

The session did not touch dohit containers, files, or ports.

### Deployment Fixes

NPM route mismatch was found and corrected by the user:

```text
dev.clipperstudio.ai        -> http://192.168.0.23:42203
dev-admin.clipperstudio.ai  -> http://192.168.0.23:42303
dev-api.clipperstudio.ai    -> http://192.168.0.23:43203
```

Angular initially rendered blank pages. Chrome headless/CDP showed Angular
`NG0908` errors because `zone.js` was installed but not included in Angular
build polyfills. The fix was applied in both Angular apps:

```json
"polyfills": [
  "zone.js"
]
```

Fix commits:

```text
clipper_web_client  e2102db fix: include Angular zone polyfill
clipper_web_admin   e21cabe fix: include Angular zone polyfill
```

Both commits are pushed to `origin/main`.

### Final Verification

Initial health checks passed:

```text
http://192.168.0.23:42203/healthz
http://192.168.0.23:42303/healthz
http://192.168.0.23:43203/v1/health
https://dev-api.clipperstudio.ai/v1/health
```

Final browser/CDP verification:

```text
https://dev.clipperstudio.ai        -> C / CLIPPER2 WEB / Coming Soon
https://dev-admin.clipperstudio.ai  -> A / CLIPPER2 ADMIN / Coming Soon
https://dev-api.clipperstudio.ai/v1/health -> JSON health response
```

Angular runtime errors were no longer present.

Temporary diagnostic files were created under `/private/tmp` on m2-stage, not
inside any repo:

```text
/private/tmp/clipper-cdp-check.mjs
/private/tmp/clipper-client.png
/private/tmp/clipper-admin.png
/private/tmp/clipper-dev-domain.png
```

## Still Not Done

- DB migrations/tables/seeds do not exist yet.
- Release metadata endpoint is still a placeholder:
  - `GET /v1/releases/latest -> 501 release_metadata_not_implemented`.
- Social login/deep-link design is not finalized.
- Payment module integration is not finalized. Current payment direction is
  manual bank transfer request/review.
- Real client-facing download/login/signup pages are being developed separately
  and are not part of the deployed placeholders yet.
- Stage/prod web/admin/API deployment has not started.

## 2026-06-08 Later Local Follow-up

After the m2-stage deployment, `clipper_web_api` gained real PostgreSQL
readiness checks.

Commit:

```text
clipper_web_api 8386865 feat: verify database connections in health checks
```

Implementation:

- Added `pg` runtime dependency and `@types/pg`.
- Added `DatabaseHealthService`.
- `/healthz` and `/v1/health` now check all three configured DB URLs with
  `SELECT 1`.
- Health returns `200` only when user/admin/release DBs are configured and
  reachable.
- Missing or unreachable DBs return `503` with non-secret status fields.
- `/v1/info` still reports configuration only and does not expose connection
  strings.

Verification:

- `clipper_web_api npm test`: passed (`4 passed`).
- `clipper_web_api git diff --check`: passed.
- Local Docker build was not verified because local Docker Desktop/daemon was
  not running.

Still remaining after this follow-up:

- Deploy `clipper_web_api` `8386865` to m2-stage and verify
  `https://dev-api.clipperstudio.ai/v1/health` reports all three DBs connected.
- DB migrations/tables/seeds do not exist yet.
- Release metadata endpoint is still a placeholder.
