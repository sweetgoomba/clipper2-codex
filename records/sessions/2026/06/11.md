# 2026-06-11

2026-06-11 Clipper2 plugin/project/queue terminology correction.

- User asked to start from `.codex/README.md` and understand current state,
  focused on `clipper_angular`, `clipper_nestjs`, `clipper_python`, and
  `clipper_electron`.
- User clarified that recent work date to consider was 2026-06-10.
- Inspected the 2026-06-10 shortform phase 3 handoff and current app code
  around plugin systems, project history, job queues, render jobs, Python
  plugin runtime, and Electron plugin hosting.
- Initial finding:
  - Current code still mixes `workflow`, `plugin`, `job`, `project`, and
    legacy `workspace`.
  - `/jobs` is still the central execution queue for several flows.
  - `VideoRenderJob` is a separate render execution model.
  - Angular Projects UI has synthetic job cards for Projects that do not have
    matching `/jobs` history.
  - Python SDK has worker-local `/jobs`; it does not own product-level project
    history.
  - Electron hosts packaged runtime processes and should not own product
    project/queue policy.

## User Product Language Decision

User rejected `workflow` as a user-facing product concept.

Confirmed product model:

- Users should understand only two core concepts:
  - `플러그인`
  - `프로젝트`
- A plugin is a Store item that users install, open, and use.
- A project is what users start creating inside a plugin.
- The queue unit users see is always a project.
- The archive/library/project page should show completed projects.
- `workflow`, `job`, `runtime`, `worker`, and `pipeline` should not be exposed
  as user-facing concepts.

Important correction:

- `shortform_prompt`, `shortform_url`, and `shortform_paste` are each separate
  user-visible plugins.
- They are not just input modes inside one shortform plugin.
- They may share internal shortform project/render implementation.

## Developer Terminology Decision

Use these concepts for new docs and future implementation planning:

| Term | Meaning |
| --- | --- |
| `Plugin` | User-visible Store feature unit |
| `Project` | User-created work item; queue/archive unit |
| `ProjectRun` | Internal execution record for processing/rendering a project |
| `RuntimeProgress` | Raw worker/provider progress event |
| `ProjectProgress` | User-facing normalized progress state |
| `ProgressStep` | User-visible analysis step message when appropriate |
| `RuntimeWorker` | Hidden Python/render/ffmpeg process or executor |

Avoid using `task` as a new product/doc term because it collides with
user-visible progress stages.

## Progress Message Decision

- Highlight analysis can show detailed user-facing progress steps.
  - Example: `문장 위치를 영상과 맞추고 있습니다`.
- Shortform/ffmpeg render should not expose technical render messages like:
  - `Staging render assets`
  - `Rendering clip 1/5`
  - `Concatenating rendered clips`
  - `Mixing render audio`
- Render UI should show simplified messages:
  - `영상 생성 중`
  - `영상 생성 완료`
  - `영상 생성 실패`
  - `영상 생성 취소됨`

## Target Direction

Move from current `/jobs`-first behavior toward Project-first behavior:

```text
Current:
Job created -> queue run -> completed -> Project created/promoted

Target:
Project created -> ProjectRun created -> queue run -> same Project updated
```

Keep existing `/jobs`, `VideoRenderJob`, and Python runtime `/jobs` as
compatibility/internal execution layers until the Project-first model is
implemented safely.

Important current status:

- The Project-first / Plugin / Queue model cleanup has **not** been implemented
  yet.
- The 2026-06-11 work established the terminology and wrote the future
  implementation plan.
- The agreed execution order is:
  1. Finish shortform legacy UI/pre-render parity first on
     `work/clipper1-input-workflow-split`.
  2. After that is stable and approved, start Project-first / Plugin / Queue
     cleanup.
- Do not mix the Project-first queue/model refactor into the current shortform
  legacy UI port.

## Documentation Updates

- Replaced the earlier terminology draft with:
  - `.codex/design/PLUGIN_PROJECT_QUEUE_TERMINOLOGY_2026-06-11.md`
- Added the Phase 1 implementation plan:
  - `.codex/design/PLUGIN_PROJECT_QUEUE_PROJECT_FIRST_IMPLEMENTATION_PLAN_2026-06-11.md`
  - Scope: Project-first facade over existing `/jobs` and `VideoRenderJob`,
    user plugin/runtime worker classification, Project lifecycle states,
    ProjectRun records, Angular Project cards, and render progress copy.
  - Out of scope for Phase 1: deleting `/jobs`, global `WorkflowExecutor`
    rename, and broad runtime architecture replacement.
  - Status: plan only. This implementation is deferred until after shortform
    legacy parity is complete.
- Marked the older easy explanation document as superseded for product terms:
  - `.codex/design/WORKFLOW_PLUGIN_JOB_EASY_EXPLANATION.md`
- Updated `.codex/README.md` to link the current product terminology.
- Updated `.codex/README.ARCHITECTURE.md` to clarify that user-facing terms are
  Plugin and Project while `WorkflowExecutor`/job terms remain current code
  names.
- Updated `.codex/handoff/NEXT.md` with the latest product terminology
  decision and 2026-06-10 repo heads.

## Repo State Noted

Verified 2026-06-10-focused local heads:

```text
.codex:           clipper1-input-workflow-docs @ 77dcb9c docs(shortform): add phase 3 session handoff
clipper_angular:  work/clipper1-input-workflow-split @ 993efb2 refactor(shortform): use project terminology
clipper_nestjs:   work/clipper1-input-workflow-split @ 8ac76d5 refactor(shortform): rename workspace state to project
clipper_python:   main @ 535131c Render sample images as cover
clipper_electron: main @ f701677 Revert "Update electron builder"
```

At the start of this documentation update, `.codex` already had unrelated
deleted `implementation/*` files in the worktree. These were not created or
reverted during this terminology update.

## Shortform Legacy Parity Scope

The user clarified that the current shortform production UI in
`work/clipper1-input-workflow-split` is not acceptable. The branch itself should
not be thrown away because it contains required changes:

- `shortform_url`, `shortform_paste`, and `shortform_prompt` remain separate
  user-visible plugins.
- Recent `workspace` -> `project` terminology cleanup remains.
- Existing Clipper2 plugin/project pre-render structure remains where possible.

The new Phase 1 target is strict legacy parity:

- The Clipper2 shortform production editor must match the legacy
  `adlight_angular` Clipper shortform screen exactly in UI, styling, and
  pre-render behavior.
- "Close enough" is not accepted; differences are bugs unless explicitly
  approved by the user.
- Internal code may differ from legacy, but user-visible UI/behavior must match.
- The legacy `D2XDataService` should not be copied as the core state model.
  Clipper2 should use its own state/service boundaries and adapt to the legacy
  UI contract.

The user also clarified the video-generation boundary:

- The `숏폼 생성하기` button must not call ffmpeg, render APIs, queue APIs, or
  `/projects` navigation in Phase 1.
- Clicking it must only build and console-log the payload that would later be
  sent to the legacy video generation path.
- The payload schema must match the legacy `adlight_python` path:
  - `app/schemas/ShortsProjectSchema.py`
  - `app/schemas/ClipSchema.py`
  - `app/routers/v1/ShortsProjectRouter.py`
  - `app/services/ShortsProjectService.py::prepare_media_server_payload`
  - `app/services/VideoService.py::create_video`

Added docs:

- `features/clipper-studio/records/2026/06/11-shortform-legacy-parity-port.md`
- `features/clipper-studio/records/2026/06/11-shortform-legacy-parity-port-plan.md`

## Shortform Legacy Parity Implementation Progress

2026-06-11 later session work moved the strict legacy parity port from planning
into implementation on the existing `work/clipper1-input-workflow-split`
branches. The branch was not restarted from `feature/initial-scaffold` because
that would lose the required shortform plugin split and project terminology
cleanup.

Current local heads checked during the documentation update:

```text
clipper_angular: work/clipper1-input-workflow-split @ 8d9d0a5 Fix shortform legacy font sizing
clipper_nestjs:  work/clipper1-input-workflow-split @ 06e3123 Implement shortform clip generation providers
.codex:          clipper1-input-workflow-docs @ 3594f97 Merge branch 'project-first-plugin-queue-docs' into clipper1-input-workflow-docs
```

Commits completed in this session:

- `clipper_angular` `a1830ad Port legacy shortform UI`
- `clipper_angular` `8d9d0a5 Fix shortform legacy font sizing`
- `clipper_nestjs` `06e3123 Implement shortform clip generation providers`
- `.codex` `b917af8 docs: update project-first terminology`
- `.codex` `3594f97 Merge branch 'project-first-plugin-queue-docs' into clipper1-input-workflow-docs`

Worktree and runtime-data cleanup:

- The temporary `.worktrees` directory was removed after its useful changes were
  applied back to the normal repo working directories.
- `.clipper_data/` was identified as local NestJS runtime data created when
  `CLIPPER_DATA_DIR` is not set.
- `clipper_nestjs/.clipper_data/` is now ignored by git.
- Packaged Clipper2 runtime data is expected under the app data directory, e.g.
  `/Users/jina/Library/Application Support/Clipper2` on macOS, not under the
  source checkout.

Angular shortform progress:

- The three shortform Store entries remain separate user-visible plugins:
  `shortform_url`, `shortform_paste`, and `shortform_prompt`.
- The shortform editor now opens inside the Clipper2 shell instead of as a raw
  standalone dev route, so the Clipper2 left sidebar remains part of the app
  frame.
- The Plugin Store detail behavior was restored: selecting a plugin shows the
  detail panel on the right, with the open action there rather than inside each
  plugin card.
- Legacy Clipper1 shortform UI/assets/styles were ported into the Clipper2
  shortform editor.
- Legacy shortform CSS was isolated from global Clipper2 styling. Only the
  shared root variables remain globally loaded; reset/common/edit styles are
  scoped to the shortform editor component.
- A style collision between legacy `.actions` CSS and the Plugin Store detail
  action area was fixed by renaming the Store detail action wrapper.
- Clip and subtitle drag/drop behavior was restored for the editor.
- Subtitle hover action flicker was reduced so add/delete/preview controls do
  not linger across several rows during fast mouse movement.
- Dragging a clip no longer creates temporary scrollbars inside the clip
  container.
- The shortform clip-generation modal is driven by realtime NestJS WebSocket
  events. Clipper2 is not using legacy SSE for this pre-render clip-generation
  flow.
- The `숏폼 생성하기` button remains Phase 1 log-only behavior: it builds/logs the
  legacy video-generation payload and does not call render, queue, or navigation
  endpoints.
- The left panel font-size regression was fixed after legacy `rem` variables
  became too large when the old global `html { font-size: 62.5%; }` reset was
  not applied. The fix uses scoped px-based font variables on
  `.shortform-legacy`, preserving global Clipper2 typography.

NestJS shortform progress:

- Shortform clip generation no longer returns fixed dummy clips as the normal
  path.
- Clip script generation uses a configured LLM script provider.
- TTS generation uses configured Naver Clova TTS.
- Media lookup uses configured Naver image search for now.
- Shortform clip-generation progress events are broadcast over WebSocket to the
  Angular modal.
- Naver image no-result cases are handled without breaking the whole clip list.
- `.env.example` documents required LLM/Naver configuration.

Verification performed during the implementation:

- `clipper_angular` focused tests passed (`17 SUCCESS`) after the legacy UI and
  style isolation changes.
- `clipper_angular npm run build` passed.
- `clipper_nestjs npm run build` passed.
- `clipper_nestjs node --test` shortform tests passed (`9 passed` in the JS
  test set and `1 passed` in the TS event test).
- Browser/CDP check confirmed the shortform left panel computed font sizes after
  the scoped fix:
  - tab text: `16px`
  - input text: `13px`
  - button text: `16px`
  - root `html` font size remains `16px`

Known verification gap:

- The full Angular suite still had a pre-existing unrelated Template Builder
  snapshot failure around `layoutImage` being undefined. That was not part of
  this shortform parity work and was not fixed here.

## Shortform Remaining Work

Strict parity is not complete yet. The current implementation is a working
foundation, but every remaining visual or behavioral mismatch against
`adlight_angular` should still be treated as a bug unless the user approves a
difference.

Immediate manual checks still needed:

- Plugin Store flow:
  - select `shortform_url`, `shortform_paste`, or `shortform_prompt`
  - confirm the right detail panel shows the expected plugin explanation
  - open the plugin from the detail panel
  - confirm the shortform editor opens while the Clipper2 sidebar remains
- URL, prompt, and paste input behavior before clip generation.
- Clip-generation modal sequence over WebSocket:
  - text analysis should light first
  - TTS/media/clip generation should advance from backend events
  - errors should surface clearly without leaving stale modal state
- Generated clip card layout, media thumbnail placeholder behavior, and
  generated asset preview behavior.
- Clip drag/drop and subtitle drag/drop behavior in the exact allowed legacy
  scope.
- Subtitle add/delete/edit/preview hover controls.
- TTS speaker/speed controls, BGM controls, title/logo controls, template/style
  controls, and preview panel behavior.
- `숏폼 생성하기` manual check:
  - logs the legacy payload
  - does not call render/video/queue endpoints
  - does not navigate to `/projects`

Remaining implementation areas:

- Continue visual parity pass against legacy reference screens for left input
  panel, center clip/subtitle editor, right style/preview panel, empty states,
  modal states, focus states, disabled states, hover states, and responsive
  behavior.
- Ensure `shortform_url`, `shortform_paste`, and `shortform_prompt` each keep
  their own user-facing plugin identity while sharing internal shortform editor
  implementation.
- Harden environment loading for all local/devapp/packaged modes so missing
  LLM, Naver Clova TTS, or Naver image search configuration fails with a useful
  app-facing message instead of confusing backend errors.
- Validate packaged app behavior separately:
  - runtime data path under Application Support
  - bundled NestJS env loading
  - WebSocket clip-generation events
  - Naver/LLM provider availability
- After the user approves UI/pre-render parity, the next major phases are:
  1. Project-first / Plugin / Queue cleanup:
     - classify user plugins and hidden runtime workers
     - make Project the user-visible queue/archive unit
     - add ProjectRun/ProjectProgress facade over current `/jobs` and
       `VideoRenderJob`
     - remove synthetic job-card behavior from the user-facing Project list
  2. Video-generation integration:
     - connect `숏폼 생성하기` to the legacy `adlight_python`
       `VideoService.py` schema path
     - keep shortform render progress user-facing copy simple, e.g. `영상 생성 중`
       rather than ffmpeg step details
