# 2026-06-18

## Shortform Template Snapshot / Edit Preview Parity

구현 커밋:

```text
clipper_nestjs:  0c78413 Preserve shortform template snapshots
clipper_angular: e850888 Use shortform template snapshots in preview
clipper_nestjs:  5c7112b Allow refreshing shortform template snapshots
clipper_angular: b1b7a34 Explain stale shortform template snapshots
```

구현:

- `clipper_nestjs`
  - `ShortformProject`에 `templateVersionSnapshot`을 추가했다.
  - project 생성 또는 `renderSettings.templateId` 변경 시 현재 Template Preset catalog 항목을
    snapshot으로 저장한다.
  - 같은 project를 나중에 편집/렌더할 때는 같은 template id의 catalog 최신본보다 저장된
    `templatePresetSnapshot`을 render recipe에 우선 적용한다.
  - 기존 render payload 구조는 유지하고, manifest `detail.editState`에 optional snapshot만
    추가했다.
- `clipper_angular`
  - shortform project model에 `templateVersionSnapshot`을 추가했다.
  - `ShortformWorkflowStore.selectedShortformTemplate()`가 project snapshot의
    `shortformTemplateRuntimeSpec`을 catalog 최신본보다 우선 반환하도록 바꿨다.
  - snapshot이 없는 신규/기존 project는 기존처럼 최신 catalog template을 사용한다.
  - Template selector 목록/thumbnail은 최신 catalog 상태를 유지한다.
  - snapshot preview와 최신 catalog runtimeSpec이 다르면 템플릿 섹션 아래에 안내를 표시한다.
  - 사용자가 `현재 템플릿 적용`을 누르면 backend에 `refreshTemplateSnapshot: true`를 보내
    같은 template id의 snapshot을 최신 catalog preset으로 갱신한다.

검증:

```text
clipper_nestjs:
- npm run build
- node --test test/simplified-shortform-render-recipe-provider.test.js test/shortform-project-generation-assets.test.js
  13 pass
- node --test test/shortform-project-api.test.js test/shortform-project-generation-assets.test.js test/simplified-shortform-render-recipe-provider.test.js test/template-builder-render-payload.test.js
  36 pass
- node --test test/shortform-project-generation-assets.test.js test/simplified-shortform-render-recipe-provider.test.js test/shortform-project-api.test.js
  24 pass
- git diff --check

clipper_angular:
- ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/shortform/pages/shortform-workflow.store.spec.ts
  7 SUCCESS
- ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/shortform/pages/shortform-workflow.store.spec.ts --include=src/features/shortform/pages/shortform-workflow-page.component.spec.ts --include=src/features/shortform/components/preview/shortform-browser-timeline-preview.component.spec.ts
  83 SUCCESS
- ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/shortform/pages/shortform-workflow-page.component.spec.ts --include=src/features/shortform/pages/shortform-workflow.store.spec.ts --include=src/features/shortform/components/preview/shortform-browser-timeline-preview.component.spec.ts
  84 SUCCESS
- npm run build
- git diff --check
```

## Shortform Project Storage / Remaining Work Audit

사용자 질문:

- `shortform/projects.json`과 `projects/projects.json`의 차이.
- `shortform/projects.json`에서 `clips: []`인 프로젝트가 보이는 이유.
- `promotedProjectId`, `shortform_project_*`, `shortform_*`, `shortform_render_*`,
  `source.shortform_project_*`의 관계.
- Template Builder 템플릿을 수정한 뒤 예전 shortform project를 다시 편집으로 열 때,
  기존 output 영상과 편집 preview가 달라질 수 있는 문제.
- 현재까지 남은 큰 작업 목록 정리와 문서화.

확인한 저장소:

```text
~/Library/Application Support/Clipper2/shortform/projects.json
~/Library/Application Support/Clipper2/projects/projects.json
~/Library/Application Support/Clipper2/templates/template-builder.json
~/Library/Application Support/Clipper2/projects/files/local/shortform_<renderProjectId>/
```

파일 역할:

- `shortform/projects.json`
  - shortform 제작/편집 원본 project store.
  - `source`, `clips`, `renderSettings`, `previewTimeline`, `mainTitle1`,
    `mainTitle2`, `promotedProjectId` 등을 저장한다.
  - `POST /projects/shortform/projects` 직후에는 의도적으로 `clips: []`,
    `status: editing`이다.
  - `POST /projects/shortform/projects/:projectId/clips`가 성공하면 clips가 채워지고
    `status: ready`가 된다.
  - 실제 앱 데이터 기준 총 38개 중 `clips: []`는 3개였고, 모두 2026-06-16 초기에
    생성만 되고 clip generation이 완료되지 않은 `editing` project였다.
- `projects/projects.json`
  - `/projects` 작업 보관함/완료 project store.
  - `projectId`, `jobId`, `pluginName`, `status`, `sourceAssets`, `outputRoot`,
    `params`, `result.video_path`, `result.thumbnail_path`, `result.manifest`,
    `artifacts`, `completedAt` 등을 저장한다.
  - 완료 card, 재생, output file resolve에 쓰인다.
- `templates/template-builder.json`
  - Template Builder family 최신본 store.
  - 현재 shortform project는 이 최신 template을 `renderSettings.templateId`로만
    참조한다.
- `projects/files/local/shortform_<renderProjectId>/render-jobs/*.clipper1-render-payload.json`
  - 실제 render job payload/recipe snapshot.
  - `legacy_payload.template_builder_render_contract`에는 렌더 당시 Template Builder
    contract snapshot이 들어 있다.

ID 관계:

```text
shortform_project_1781755507534
= shortform 제작/편집 원본 project id

shortform_1781755534088
= 보관함/렌더 산출물 project id
= projects/files/local/shortform_1781755534088/ 폴더명

shortform_render_1781755534088
= queue/render job id

source.shortform_project_1781755507534
= completed manifest/sourceAssets에서 원본 shortform project를 가리키는 source asset id

promotedProjectId
= shortform 원본 project에서 연결된 보관함/렌더 project id
```

현재 구조 판단:

- 렌더 결과물 보존은 괜찮다.
  - 완료 render payload에는 `legacy_payload.template_builder_render_contract`가 있어
    생성 당시 Template Builder contract snapshot으로 render된 결과가 보존된다.
- 편집 재진입 관점은 부족하다.
  - `shortform/projects.json`에는 현재 `renderSettings.templateId`만 있고,
    생성/렌더 당시 template snapshot은 없다.
  - Template Builder 최신본을 수정한 뒤 예전 shortform project를 편집으로 열면,
    output 영상은 예전 template 기준인데 편집 preview는 최신 template 기준으로 보일 수 있다.

이번에 정리한 남은 작업:

1. Shortform project template snapshot/version snapshot
   - `shortform/projects.json` 원본 project에 선택 당시 Template Builder snapshot 또는
     version snapshot을 저장한다.
   - 편집 화면은 기본적으로 snapshot을 우선 사용한다.
   - 현재 최신 template과 다를 경우 표시/옵션을 제공한다. 예: `현재 템플릿으로 업데이트`.
2. Project-first / Plugin / Queue 전체 정리
   - `shortform_project_*` 원본 project, `shortform_*` promoted/completed project,
     `shortform_render_*` job의 ownership를 명확히 분리한다.
   - 사용자-facing 단위는 Project와 ProjectRun으로 정리하고 `/jobs`는 내부 호환 계층으로
     둔다.
   - `clipper1_video_render`는 사용자 plugin이 아니라 hidden RuntimeWorker로 정리한다.
   - progress/status/cancel/retry/completed 이동을 Project-first 모델에 맞춘다.
3. 작업 보관함 non-shortform detail flow
   - 현재 shortform 완료 card는 hover action overlay + 편집/재생 경로를 가진다.
   - 하이라이트 등 다른 plugin project는 기존 오른쪽 inline detail panel 제거 이후
     상세 output을 보여줄 plugin별/detail page가 필요하다.
4. 작업 보관함 completed card UI 정리
   - Clipper2 output 기본값은 숏폼이므로 card thumbnail은 9:16 비율을 기본으로 한다.
   - shortform은 렌더 시 저장된 `main_thumbnail.jpg`를 사용한다.
   - non-shortform thumbnail 정책은 별도 결정이 필요하다.
     후보: plugin-provided thumbnail, source video frame, first output artifact preview,
     workflow-specific fallback.
   - card에 표시할 정보와 배치를 다시 설계한다. 예: title, plugin/source mode, duration,
     clip count, created/completed time, template/ratio, status badges.
5. 남은 renderer/app QA
   - packaged app에서 Template Builder custom preset end-to-end render QA.
   - video/thumbnail/manifest/artifact path가 앱 재시작 후에도 열리는지 검증.
   - Python worker lifecycle: dev/packaged start/stop, port/baseUrl, event delivery,
     output_root access.
   - 더 이상 쓰지 않는 `VideoRenderJobsService`/old render provider path 제거 전 grep.
6. Preview/URL/fixture follow-up
   - Angular current branch preview snapshot export test 2개 실패는 별도 Angular 작업으로 남김.
   - URL extractor selector edge case와 실제 URL QA 보강.
   - 구형 21개 Clipper1 golden/baseline fixture는 현재 shortform 생성 경로가 아니므로
     이번 목록에서도 별도/후순위로 유지.

## Template Builder Subtitle Height Auto-Sizing

사용자 QA:

- Template Builder에서 자막 폰트 크기를 키우면 `fontSize`만 바뀌고 자막 레이어 높이는
  그대로라 글자 아랫부분이 잘린다.
- 그 상태로 템플릿을 저장하고 shortform render에 사용하면 최종 output에서도 자막
  아랫부분이 잘린다.

원인:

- Angular Template Builder의 공통 스타일 패치는 `text.fontSize`, outline/shadow/box
  style만 draft family에 적용하고, `subtitleText.height`와 `subtitleText.box.height`는
  갱신하지 않았다.
- Python subtitle artifact renderer도 저장된 `box.height`를 그대로 신뢰해, 이미 작게
  저장된 템플릿은 큰 폰트/윤곽선이어도 작은 box 안에서 clipping됐다.

구현 커밋:

```text
clipper_angular: 37dea74 Auto-size subtitle height for template typography
clipper_python:  5eda200 Guard subtitle artifact height for large typography
```

구현:

- `clipper_angular`
  - Template Builder common style apply 단계에서 `role === 'subtitleText'`일 때만
    최소 line box height를 계산한다.
  - 최소 높이는 `fontSize * lineHeight`, outline width, shadow outline/offsetY,
    box border width를 반영한다.
  - 기존 높이가 더 크면 줄이지 않고, 필요한 경우에만 `subtitleText.height`와
    `subtitleText.box.height`를 같이 키운다.
- `clipper_python`
  - `render_template_builder_subtitle_artifact()`에서 저장된 box height와 계산된 최소
    line box height 중 큰 값을 사용한다.
  - 이미 작게 저장된 기존 템플릿도 최종 artifact/render에서 clipping되지 않도록
    방어한다.

검증:

```text
clipper_angular:
- ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/template-builder/pages/template-builder-page.component.spec.ts
  72 SUCCESS
- npm run build
- git diff --check

clipper_python:
- uv run pytest tests/test_template_builder_subtitle_artifacts.py -q
  4 passed
- uv run pytest tests/test_clipper1_video_render_template_styles.py -q
  31 passed
- uv run pytest tests/test_clipper1_video_render_text_artifact_job.py tests/test_template_builder_text_artifacts.py tests/test_template_builder_frame_artifacts.py -q
  9 passed
- git diff --check
```

## Shortform TTS Duration Truncation Follow-Up

사용자 QA:

- 새로 만든 shortform의 마지막 대사 `큰 환경 보호가 가능해요! 함께 실천해요.` 중
  TTS가 `큰 환경 보호가 가능해요!`까지만 재생됐다.

원인:

- 최신 job `shortform_render_1781743028087`의 payload에는 마지막 subtitle text와
  `tts_url`이 온전하게 들어 있었다.
- 실제 TTS 파일
  `tts.shortform_project_1781743003682.clip.7.line.clip.7.1.1781743024154.mp3`은
  ffprobe 기준 `2.760s`였지만, `shortform/projects.json`과 render payload에는
  `durationMs=1981` / `duration=1.981`로 저장돼 있었다.
- 저장된 `1981ms`는 `estimatedTtsDurationMs()` fallback 공식과 정확히 일치했다.
  즉 packaged/backend 환경에서 ffprobe duration 측정이 실패했고, 짧은 추정 duration이
  저장됐다.
- Python render worker는 subtitle duration을 믿고 TTS segment에
  `atrim=0:1.981`을 적용하므로 뒤 문장이 잘렸다.

구현:

- `clipper_nestjs`
  - `NaverClovaShortformTtsProvider`가 ffprobe를 사용할 수 없을 때도 TTS 응답 buffer에서
    WAV/MP3 duration을 직접 계산하도록 fallback parser를 추가했다.
  - 기존 프로젝트처럼 이미 짧은 duration이 저장된 경우도 다시 렌더하면 고쳐지도록,
    render job 준비 시 local TTS 파일의 실제 duration을 읽어 in-memory render project의
    narration line duration, clip duration, media slots를 보정한다.
  - 저장 project 구조는 바꾸지 않고 render manifest/payload 생성에 쓰는 project copy만
    보정한다.

검증:

```text
- npm run build
- node --test test/shortform-tts-provider.test.js
  1 pass
- node --test test/shortform-project-generation-assets.test.js --test-name-pattern "shortform render manifest stretches"
  6 pass
- node --test test/shortform-tts-provider.test.js test/shortform-project-generation-assets.test.js test/shortform-project-api.test.js
  17 pass
- Actual latest MP3 parser check:
  audioDurationMsFromBuffer(...clip.7.line.1.mp3) = 2784ms
- Actual stored project manifest check with new render path:
  last clip subtitles: 1.584s + 2.784s
  last media slot endMs: 4368
  outputDurationSec: 29.76
```

## App FFmpeg/FFprobe Env Contract

사용자 정정:

- `/opt/homebrew/bin/ffprobe` 같은 개발자 로컬 설치본은 앱 런타임 기준이 아니다.
- packaged 앱은 `userData/bin`에 설치한 앱 전용 ffmpeg/ffprobe를 전체 런타임에서
  사용해야 한다.
- 표준 env는 `FFMPEG_BIN`, `FFPROBE_BIN`으로 통일하고, `IMAGEIO_*`,
  `CLIPPER_FFMPEG_BIN`, `CLIPPER_YTDLP_FFMPEG_LOCATION` 같은 별도 이름은 직접 사용하지
  않는 방향으로 정리한다.

앱 전용 설치 경로:

```text
macOS: ~/Library/Application Support/Clipper2/bin/ffmpeg
macOS: ~/Library/Application Support/Clipper2/bin/ffprobe
```

구현 커밋:

```text
clipper_electron: d461dfd Pass app ffmpeg tools to runtimes
clipper_nestjs:  b4739c6 Use app ffmpeg env in NestJS
clipper_python:  c2e54c7 Use standard ffmpeg env in plugins
```

구현 내용:

- `clipper_electron`
  - packaged NestJS startup preflight에서 `ensureFfmpeg()`를 호출한다.
  - `getFfmpegPaths()`의 앱 전용 경로를 NestJS process env에
    `FFMPEG_BIN`, `FFPROBE_BIN`으로 주입한다.
  - NestJS process `PATH` 앞에도 앱 전용 `userData/bin`을 추가한다.
  - Python plugin process도 `FFMPEG_BIN`, `FFPROBE_BIN`을 받도록 바꾸고
    `IMAGEIO_FFMPEG_EXE`, `IMAGEIO_FFPROBE_EXE` 주입은 제거했다.
- `clipper_nestjs`
  - TTS duration probe는 `FFPROBE_BIN`을 사용한다.
  - `simple_ffmpeg_transform`은 `FFMPEG_BIN`만 표준으로 사용하고
    `CLIPPER_FFMPEG_BIN` fallback을 제거했다.
  - source ingest/yt-dlp 병합은 `FFMPEG_BIN`에서 dirname을 계산해
    `--ffmpeg-location`으로 넘긴다. `CLIPPER_YTDLP_FFMPEG_LOCATION` 직접 config는 제거했다.
- `clipper_python`
  - `clipper1_video_render`, `dance_highlight`, `dialog_highlight`,
    Template Builder frame extraction script의 직접 ffmpeg/ffprobe lookup을
    `FFMPEG_BIN`, `FFPROBE_BIN`으로 바꿨다.

검증:

```text
clipper_nestjs:
- npm run build
- node --test test/shortform-tts-provider.test.js
  2 pass
- node --test test/shortform-tts-provider.test.js test/shortform-project-generation-assets.test.js test/workflow-executor-registry.test.js test/simplified-shortform-local-ffmpeg-render-provider.test.js
  11 pass
- git diff --check

clipper_electron:
- npm run build
- git diff --check

clipper_python:
- uv run pytest tests/test_template_builder_video_frame_extraction.py tests/test_clipper1_video_render_contract.py::test_local_render_adapter_uses_standard_ffmpeg_env -q
  3 pass
- uv run pytest tests/test_template_builder_video_frame_extraction.py tests/test_clipper1_video_render_contract.py::test_local_render_adapter_uses_standard_ffmpeg_env tests/test_clipper1_video_render_media_looping.py -q
  30 pass
- git diff --check
```

## URL Shortform Clip Generation 500 Follow-Up

사용자 QA:

- packaged app에서 URL 숏폼 제작 페이지에
  `https://blog.naver.com/fall38/224294202770` 입력 후 `클립 생성하기`를 눌렀을 때
  Angular가
  `Http failure response for http://127.0.0.1:61940/v1/projects/shortform/projects/shortform_project_1781746600126/clips: 500 Internal Server Error`
  를 표시했다.

확인:

- 같은 running NestJS endpoint를 직접 재호출했다.
- 재호출은 201 Created로 성공했고, `shortform_project_1781746600126`은 `ready`,
  clips 8개, Naver Blog parsed text/source images, TTS artifact가 모두 저장됐다.
- 저장된 TTS duration은 실제 ffprobe 기반 값으로 보이며, 이전처럼 fallback 공식에
  고정된 패턴은 아니었다.
- 앱 데이터 디렉터리에는 첫 500의 stack trace가 남아 있지 않았다.

추정 원인:

- URL 파싱/저장 로직은 같은 입력으로 성공했다.
- 첫 500은 OpenAI script generation, Naver Clova TTS, 네트워크 fetch 같은 외부 provider
  transient failure가 plain `Error`로 올라와 NestJS에서 generic 500이 된 케이스로 본다.

구현 커밋:

```text
clipper_nestjs: 095e61d Map shortform clip provider errors
```

구현:

- `ShortformProjectService.generateClips()` catch에서 이미 의도된 NestJS
  `HttpException`은 그대로 유지한다.
- provider/network plain `Error`는 `ServiceUnavailableException`으로 감싸서 500 대신
  503으로 응답하고 원인 메시지를 보존한다.
- 회귀 테스트를 추가했다.

검증:

```text
- 직접 running app endpoint 재호출:
  POST /v1/projects/shortform/projects/shortform_project_1781746600126/clips
  201 Created, clips=8
- npm run build
- node --test test/shortform-project-generation-assets.test.js
  7 pass
- node --test test/shortform-project-api.test.js test/shortform-project-generation-assets.test.js test/shortform-tts-provider.test.js
  19 pass
- git diff --check
```

## Shortform Render TTS Tail + Outline Subtitle Fix

사용자 QA:

- 최근 생성 영상 중 제목이 `삼길포 대호방조제`로 시작하는 결과에서 마지막 자막
  `곧 큰 우럭도 기대해 볼 수 있겠습니다`의 TTS가 끝까지 재생되지 않고
  `...있겠습니` 부근에서 영상이 끝났다.
- 가장 최근 추가한 세 번째 Template Builder 템플릿은 흰색 자막 + 오렌지색 윤곽선이어야
  하지만 shortform preview에서는 글자가 거의 오렌지색으로 보였다.
- 같은 템플릿으로 실제 렌더하면 자막이 아예 overlay되지 않았다.

확인:

- 실제 render wrapper:
  `~/Library/Application Support/Clipper2/projects/files/local/shortform_1781747569696/render-jobs/shortform_render_1781747569696.clipper1-render-payload.json`
- payload subtitle duration 총합은 `33.336s`였지만 실제 `main.mp4`는 ffprobe 기준
  `33.200s`였다.
- 최신 outline 템플릿 payload
  `shortform_render_1781747941693.clipper1-render-payload.json`의 `subtitleText`는
  `box.enabled=false`, `subtitleBox.visible=false`, `outline.width=8`,
  `outline.color=#ff751a`였다.
- 수정 전 subtitle artifact는 alpha bbox가 `None`인 완전 투명 PNG로 생성됐다.

원인:

- auto zoom/pan frame count가 `int(duration * 30)` floor 계산이라 segment video가
  payload duration보다 짧아질 수 있었다.
- TTS/BGM concat은 AAC segment를 concat demuxer + `-c copy`로 붙여 timestamp/tail 손실
  가능성이 있었다.
- 최종 mux에 `-shortest`가 들어 있어 video 쪽이 조금이라도 짧으면 마지막 audio tail을
  잘랐다.
- Template Builder subtitle artifact 생성 시 `subtitleBox`가 숨겨져 있고
  `subtitleText.box.enabled=false`인 템플릿을 1px fallback box로 처리해 텍스트가 전부
  clipping됐다.
- Angular preview는 `-webkit-text-stroke`를 실제 fill text에 직접 적용해 stroke가 굵은
  윤곽선에서 글자 내부를 거의 덮었다.

구현 커밋:

```text
clipper_python:  d743cdd Fix shortform TTS tail and outline subtitles
clipper_angular: 184c96f Fix shortform caption outline preview
```

구현:

- `clipper_python`
  - auto motion frame count를 `ceil(duration * 30)`로 바꿔 fractional duration을 덮는다.
  - AAC TTS/BGM segment concat은 filter `concat=n=...:v=0:a=1`로 re-encode한다.
  - 최종 audio mux에서 `-shortest`를 제거했다.
  - subtitle box가 비활성/숨김인 Template Builder 템플릿은 `subtitleText` layer 자체의
    height와 style을 fallback으로 사용한다.
- `clipper_angular`
  - preview caption fill에는 stroke를 직접 적용하지 않는다.
  - caption span의 `::before` pseudo layer가 `data-text`로 outline/shadow를 뒤쪽에 그리고,
    실제 text node는 흰색 fill로 위에 남긴다.

검증:

```text
clipper_python:
- uv run pytest tests/test_clipper1_video_render_contract.py tests/test_clipper1_video_render_media_looping.py tests/test_clipper1_video_render_motion.py tests/test_clipper1_video_render_template_styles.py -q
  72 passed
- Actual latest outline payload subtitle artifact:
  image_size=(877, 171)
  alpha_bbox=(10, 8, 867, 171)
  line_frames=[{'x': 102, 'y': 1336, 'width': 877, 'height': 70}, {'x': 380, 'y': 1437, 'width': 319, 'height': 70}]
- git diff --check

clipper_angular:
- ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/shortform/components/preview/shortform-browser-timeline-preview.component.spec.ts
  18 SUCCESS
- npm run build
- git diff --check
```

## Shortform Archive Edit Source Mode Fix

사용자 QA:

- 보관함에서 `붙여넣기로 숏폼 제작`으로 생성된 완료 프로젝트의 `편집` 버튼을 누르면
  `/shortform/prompt?project=...`가 열렸다.
- 기대 동작은 해당 프로젝트의 원래 입력 방식인 `/shortform/paste?project=...`로 열려
  붙여넣기 제작 화면에 데이터가 복원되는 것이다.

확인:

- 실제 shortform project `shortform_project_1781747847730`는
  `shortform/projects.json` 기준 `source.mode = paste`로 저장돼 있었다.
- 하지만 완료 render project/job의 manifest에는
  `detail.input.mode = editor`로 저장돼 있었다.
- Angular 보관함 route 결정 함수는 `detail.input.mode`가 `url` 또는 `paste`일 때만
  해당 route를 쓰고, 그 외 값은 `prompt`로 fallback했다.
- 같은 완료 record의 `sourceAssets[0].metadata.sourceMode`에는 `paste`가 남아 있었다.

원인:

- NestJS `ShortformProjectService.legacyInputModeFor()`가 `paste`를 generic `editor`로
  매핑했다.
- Angular `ProjectsComponent.shortformInputModeForJob()`는 기존 완료 기록의
  `sourceAssets.metadata.sourceMode`를 fallback으로 보지 않았다.

구현 커밋:

```text
clipper_angular: 38c41b6 Fix shortform archive edit source mode
clipper_nestjs:  4d5aa36 Preserve paste shortform render input mode
```

구현:

- `clipper_angular`
  - manifest detail input mode가 `prompt/url/paste`가 아닐 때
    completed project `sourceAssets`, manifest `sourceAssets`, job `params.source_assets`의
    `metadata.sourceMode`를 순서대로 확인한다.
  - 기존 완료 기록처럼 `detail.input.mode=editor`여도 source metadata가 `paste`면
    `/shortform/paste?project=...`로 이동한다.
- `clipper_nestjs`
  - `ClipperStudioProjectDetail.input.mode` union에 `paste`를 추가했다.
  - shortform render manifest 생성 시 `paste` source mode를 `editor`로 바꾸지 않고
    그대로 저장한다. `manual`만 generic `editor`로 남긴다.

검증:

```text
clipper_angular:
- RED: ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/shell/projects/projects.component.spec.ts
  Expected 'prompt' to be 'paste'.
- ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/shell/projects/projects.component.spec.ts --include=src/shell/projects/projects-history-list.component.spec.ts
  3 SUCCESS
- npm run build
- git diff --check

clipper_nestjs:
- RED: node --test test/shortform-project-generation-assets.test.js --test-name-pattern "preserves paste input mode"
  'editor' !== 'paste'
- npm run build
- node --test test/shortform-project-generation-assets.test.js --test-name-pattern "preserves paste input mode"
  8 pass
- node --test test/shortform-project-generation-assets.test.js test/shortform-project-api.test.js
  18 pass
- git diff --check
```

## Shortform Preview Caption Outline Fill Follow-Up

사용자 QA:

- 이전 수정처럼 caption outline을 `::before` pseudo layer로 뒤에 그려도, 프리뷰에서는 여전히
  흰색 fill이 오렌지 윤곽선 색으로 거의 덮여 보였다.

원인:

- `::before` pseudo element에 `-webkit-text-stroke`를 적용하는 방식은 브라우저 paint order와
  stroke 중심선 렌더링 때문에 실제 fill text 위로 올라오거나 내부를 침범해 보일 수 있었다.
- 기존 테스트는 `data-text`와 CSS var 존재만 확인해 실제 “fill을 덮지 않는 렌더 방식”을
  검증하지 못했다.

구현 커밋:

```text
clipper_angular: 5175a9d Fix shortform caption outline fill preview
```

구현:

- caption preview에서 `data-text` / `::before` / `-webkit-text-stroke` 방식을 제거했다.
- caption line text 자체에 여러 방향 `text-shadow`를 적용해 outline을 근사한다.
  CSS text-shadow는 fill text 뒤에 그려지므로 흰색 fill을 덮지 않는다.
- 기존 shadow 설정은 outline shadow 뒤에 이어 붙인다.

검증:

```text
- RED: ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/shortform/components/preview/shortform-browser-timeline-preview.component.spec.ts
  Expected data-text to be null, --preview-text-outline to be empty, textShadow to contain outline color.
- ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/shortform/components/preview/shortform-browser-timeline-preview.component.spec.ts
  18 SUCCESS
- npm run build
- git diff --check
```

## Shortform Render Remote Media Materialization

사용자 QA:

- 가장 최근 생성한 숏폼 프로젝트가 render 실패했다.
- 확인 대상은 `shortform_project_1781763078133`, render job은
  `shortform_render_1781763108805`였다.

확인:

- job status는 failed였고 error는 다음 원격 이미지 다운로드 실패였다.
  `https://thumb.tv.zumst.com/s/zum.tv-zum-cf/kbs/2019/03/11/e5877c88ddd29c3d587ed42cc25ce1ee.png`
- 해당 URL은 7번째 clip의 Naver 이미지 검색 결과 asset이었다.
- render output folder는 비어 있었다.
- 추가로 원본 `shortform_project_*`는 job 실패 후에도 `status=rendering`으로 남아 있었다.

결론:

- 편집 중 clip media 후보를 매번 저장하는 것은 아직 asset 교체 UI/검색 흐름과 맞지 않는다.
- 하지만 `숏폼 생성하기` 이후 NestJS가 render 준비를 맡는 시점에는 선택된 asset이 확정되므로,
  Python worker에는 원격 URL 대신 로컬 파일 path를 넘기는 것이 맞다.

구현 커밋:

```text
clipper_nestjs: 1740bd7 Materialize shortform render media before worker
```

구현:

- `ShortformProjectService.prepareReservedRenderJob()`에서 TTS duration 보정 후,
  asset preparer/manifest/legacy payload 생성 전에 선택된 원격 clip media를 output root의
  `draft/assets/remote`로 다운로드한다.
- 선택된 media slot asset만 다운로드하고, 같은 clip의 선택되지 않은 후보 asset은 건드리지 않는다.
- 다운로드 성공 시 render-only project copy의 asset에 `sourcePath`, media type, size/checksum을
  채워 manifest/legacy payload가 local source-file을 참조하게 한다.
- 다운로드 실패 시 해당 remote asset을 render copy에서 제외해 `remote-url` artifact가 Python worker로
  내려가지 않게 하고, 기존 prepared local fallback media로 slot을 재스케줄한다.
- render manifest가 clip `mediaPool.assets` 전체가 아니라 실제 render 선택 asset만 artifact로
  materialize하게 바꿨다.
- worker job이 failed/cancelled terminal status가 되면 원본 shortform project의 `status=failed`와
  `lastError`를 동기화한다.

검증:

```text
clipper_nestjs:
- RED: npm run build && node --test test/shortform-project-generation-assets.test.js
  new tests failed with missing withRemoteClipMediaForRender/syncShortformProjectAfterRenderJob
- npm run build && node --test test/shortform-project-generation-assets.test.js
  14 pass
- node --test test/shortform-project-generation-assets.test.js test/shortform-project-api.test.js test/simplified-shortform-render-recipe-provider.test.js
  27 pass
- sandbox 재실행에서는 API 테스트가 127.0.0.1 listen EPERM으로 실패해 외부 실행으로 재검증:
  node --test test/shortform-project-generation-assets.test.js test/shortform-project-api.test.js test/simplified-shortform-render-recipe-provider.test.js
  27 pass
- git diff --check
```

## Stale Template Snapshot Notice False Positive

사용자 QA:

- Template Builder에서 템플릿을 수정한 뒤 새롭게 숏폼을 생성하고, 생성 완료 후 편집으로 열었는데도
  “이 프로젝트는 생성 당시 템플릿으로 미리보기 중입니다...” 안내가 떴다.
- 기대 동작은 “해당 템플릿으로 영상 생성 → 그 이후 템플릿 수정 → 기존 영상 편집 재진입”일 때만
  안내가 뜨는 것이다.

원인:

- Angular `ShortformWorkflowStore.hasTemplateSnapshotMismatch()`가 snapshot runtimeSpec과 최신
  catalog runtimeSpec의 JSON 차이만으로 stale 여부를 판단했다.
- Template Builder preset catalog 응답에는 최신 템플릿의 수정 시각이 내려오지 않았기 때문에,
  “snapshot 이후 실제로 수정됐는지”를 프론트가 알 수 없었다.
- 그래서 템플릿 수정 후 새로 만든 프로젝트도 snapshot/catalalog 직렬화 차이가 있으면 stale로
  오판할 수 있었다.

구현 커밋:

```text
clipper_nestjs:  3165b79 Expose template preset update timestamps
clipper_angular: e3b6f32 Gate stale template snapshot notice by template update time
```

구현:

- `clipper_nestjs`
  - `TemplatePreset` DTO에 optional `updatedAt`을 추가했다.
  - Template Builder published preset source가 variant/family `updatedAt`을 preset에 포함한다.
- `clipper_angular`
  - `TemplatePresetResponse`, `ShortformTemplateCatalogItem`, snapshot preset model에 optional
    `updatedAt`을 추가했다.
  - template catalog item에 backend `updatedAt`을 보존한다.
  - stale 안내 조건을 “latest template `updatedAt` > snapshot `capturedAt` AND runtimeSpec differs”로
    변경했다.
  - `updatedAt`이 없거나 파싱 불가능하면 안내를 띄우지 않는다. 이쪽이 새 프로젝트 오탐을 막는
    보수적 fallback이다.

검증:

```text
clipper_angular:
- RED: ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/shortform/pages/shortform-workflow.store.spec.ts
  Expected true to be false.
- ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/shortform/pages/shortform-workflow.store.spec.ts
  9 SUCCESS
- ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --include=src/features/shortform/pages/shortform-workflow-page.component.spec.ts --include=src/features/shortform/services/shortform-project.service.spec.ts
  68 SUCCESS
- npm run build
- git diff --check

clipper_nestjs:
- RED: npm run build && node --test test/template-builder-shortform-preset-source.test.js
  preset updatedAt was undefined
- npm run build && node --test test/template-builder-shortform-preset-source.test.js
  2 pass
- node --test test/template-builder-shortform-preset-source.test.js test/shortform-project-generation-assets.test.js test/simplified-shortform-render-recipe-provider.test.js
  19 pass
- npm run build
- git diff --check
```

## Old VideoRenderJobsService Path Audit

사용자 요청:

- 다음 작업으로 Project-first / Plugin / Queue 정리의 첫 slice를 진행하되, 새 브랜치에서 작업한다.
- 추천한 첫 slice는 `VideoRenderJobsService` / old render-provider path 제거 가능성 audit이다.

브랜치:

```text
clipper_nestjs: audit/old-render-path-cleanup
.codex:         docs-old-render-path-cleanup
```

확인:

- shortform `숏폼 생성하기` path는 `ShortformProjectService.startRender()`에서
  `JobsService.reserve()` / `activateReserved()`로 `clipper1_video_render` queue job을 만든다.
- shortform controller route는 `/projects/shortform/projects/:projectId/render-jobs`이며,
  old `VideoRenderJobsService`를 inject하지 않는다.
- `VideoRenderJobsService`는 아직 아래 generic project output render API에 연결돼 있다.
  - `ProjectsController @Post(':projectId/outputs/:outputId/render-jobs')`
  - `ProjectsController @Get(':projectId/outputs/:outputId/render-providers')`
  - `ProjectsController @Get(':projectId/render-jobs')`
  - `ProjectsController @Delete(':projectId/render-jobs/:renderJobId')`
  - Angular `ProjectHistoryService.startVideoRenderJob()`, `listVideoRenderJobs()`,
    `cancelVideoRenderJob()`
- 따라서 `VideoRenderJobsService` 자체는 즉시 삭제 대상이 아니다. 삭제하면 non-shortform/generic
  project output render API가 깨질 수 있다.

구현:

- `clipper_nestjs/test/shortform-project-api.test.js`의 shortform render API test에
  `CLIPPER_DATA_DIR/render-jobs/render-jobs.json`이 생성되지 않는 assertion을 추가했다.
- 이 파일은 old `JsonVideoRenderJobRepository` 저장소이므로, 생성되면 shortform render가 다시
  old path로 새는 회귀를 잡을 수 있다.
- `ProjectsService.listVideoRenderProviders()`와 `ProjectsService.startVideoRenderJob()`에
  shortform manifest guard를 추가했다.
  - `manifest.workflow.id === 'workflow.shortform'`이면 old `VideoRenderJobsService`로 넘기지 않고
    `BadRequestException('Shortform render jobs must use the shortform project render API')`를 반환한다.
  - shortform 재렌더는 `/projects/shortform/projects/:projectId/render-jobs` -> `/jobs` queue path만
    사용하도록 경계를 고정했다.

검증:

```text
clipper_nestjs:
- RED: npm run build && node --test test/project-output-render-path.test.js
  Missing expected rejection.
- npm run build && node --test test/project-output-render-path.test.js
  1 pass
- node --test test/project-output-render-path.test.js test/shortform-project-api.test.js test/simplified-shortform-render-recipe-provider.test.js
  14 pass
- npm run build && node --test test/shortform-project-api.test.js
  10 pass
```

결론:

- `VideoRenderJobsService` 자체는 generic project output render API 때문에 아직 삭제하지 않는다.
- 대신 shortform workflow가 generic/old output render API로 들어가는 경로는 production guard로 막았다.
- 다음 결정 지점은 generic output render API를 `/jobs` queue로 통합할지, non-shortform project detail
  flow가 정리될 때까지 유지할지다.
