# 2026-08-21

## 사용자 요구

- 이미 구현된 토스페이먼츠 PG 전체 연동을 실제 공용 테스트키로 직접 검증한다.
- Basic→Pro 상향 전에 정확한 차액을 보여주고 명시적 결제 동의를 받는다.
- 결제 완료 정보와 새로고침 없는 이용권·크레딧 갱신을 제공한다.
- `/app` 구독 결제 정보의 비정상적인 큰 빈 공간과 우측 쏠림을 고친다.
- 구현·자동 테스트·수동 테스트·잔여 작업을 다음 세션용으로 문서화한다.

## 원인과 결정

- 기존 plan-change는 서버가 빌링키로 즉시 청구하면서 정확한 차액을 사전에 보여주지
  않았다.
- 성공 뒤 subscription만 갱신해 access/credit은 새로고침 전까지 stale했다.
- 구독 카드가 한쪽 정렬 구조여서 결제 정보가 우측에 몰렸다.
- 해결은 `quote → exact consent → confirm` 2단계 계약, 5분 견적, source fingerprint,
  one-winner charge claim, 네 projection 재조회, 반응형 패널 레이아웃으로 확정했다.

## 주요 구현

- API quote endpoint와 필수 `quoteOrderId` confirm 계약
- `upgrade_quote_source_fingerprint` forward migration/ORM mapping
- owner/target/source/expiry/status/purpose 검증과 동시 charge fencing
- safe payment completion projection과 OpenAPI closed schema
- 웹의 정확한 금액 확인창, 완료 정보, 409/503 안내
- access/credits/subscription/payment-method `Promise.allSettled` 재조회
- 구독 summary/payment-method/plan-change 반응형 패널
- 다른 세션의 stale director policy를 보존하는 migration compatibility mapping
- 공식 `data` envelope를 수용하고 문서에 없는 평면형을 거부하는 `BILLING_DELETED`
  webhook 정규화
- durable webhook inbox 복구를 실제 매분 Nest cron에 연결

## 커밋

API:

- `a4f23c9`, `cec6eaf`, `9bf7c97` — 설계·계획
- `f1cc503` — 견적 상태 영속화
- `a0f14bf` — 견적 동의 필수화
- `314a84e` — quote/confirm HTTP 계약
- `b5e3002` — 변경 경로 포맷
- `32f5f74` — director policy migration 호환성
- `f01d38d` — 실제 `data` billing-deletion webhook 수신 추가
- `004af85` — 공식 `data` envelope 단일 계약으로 정정
- `bf35efd` — webhook inbox recovery cron 등록 복원

고객 웹:

- `415813d` — quote API/model/mock
- `caadc6a` — 정확한 차액 동의와 완료 projection
- `bb87a9a` — 구독 대시보드 레이아웃

## 검증

- API 전체 Jest `154/154` suites, `1,328/1,328` tests PASS
- API build, 변경 production lint, diff/secret scan PASS
- 실제 ngrok payload 형태를 사용한 webhook 회귀 테스트 RED→PASS
- 실제 Nest schedule 등록과 tick 실행 회귀 테스트 RED→PASS
- 고객 웹 전체 Karma `170/170`, app tsc, production build PASS
- PostgreSQL 16에서 admin migration 43개와 compiled service/repository probe PASS
- quote provider/cipher 호출 0, concurrent charge claim winner 1
- expiry, foreign owner, billing fingerprint drift, purpose fence PASS

## 사용자 수동 확인

- Basic 최초 정기결제와 100 Credits 단건결제
- 구독 결제수단 변경
- Basic→Pro 견적 `₩20,000`, 추가 크레딧 600
- 명시적 확정 뒤 완료 상품·금액·주문번호·결제시각
- 새로고침 없는 Pro access/credit/subscription 갱신
- 자동 결제 취소와 재시작
- Pro→Basic 예약과 예약 취소
- 결제내역의 Basic `₩19,900`, upgrade `₩20,000` 두 건
- 개발자센터 테스트 결제내역의 MID-scoped billing 결제 네 건
- ngrok의 webhook POST 다섯 건과 최신 `BILLING_DELETED`의 pre-fix 400 응답
- 개발자센터 재시도 성공, ngrok HTTP 200, fingerprint-only inbox 저장과 cron 분류
- 공용 테스트키는 가상 승인이라 카드사 문자·실제 청구 없음

## webhook 정정과 실제 재전송

- webhook은 미도착이 아니었다. ngrok inspector에 POST 다섯 건이 있었다.
- 최신 요청은 공식 JSON 예시와 동일하게 `billingKey`와 `reason`을 `data` 아래에 담은
  `BILLING_DELETED`였고, 기존 closed parser가 문서를 오독해 최상위 필드를 기대하면서
  400을 반환했다.
- 공식 `data` envelope만 같은 내부 DTO로 정규화하고 문서에 없는 평면 입력은 거부하도록
  정정했다.
- 테스트에서는 평면 입력 거부가 dual parser에서 실패하는 것을 먼저 확인한 뒤, focused
  16/16, 당시 전체 1,327/1,327, build, 대상 ESLint를 통과했다. cron 회귀 테스트 추가
  뒤 최종 전체 수는 1,328/1,328이다.
- 수정본 API를 새 55420 disposable admin DB와 함께 실행하고 개발자센터의 동일 delivery를
  재시도해 성공/HTTP 200을 확인했다.
- inbox에는 raw 빌링키 없이 64자리 fingerprint만 저장됐다. 행이 `received`에 머문 것을
  계기로 recovery cron 등록 누락을 발견했고, 실제 Nest schedule tick 테스트를 RED로
  확인한 뒤 매분 cron을 연결했다.
- 다음 tick은 행을 `manual_review`/`WEBHOOK_BILLING_KEY_UNMATCHED`로 분류했다. replay DB에
  과거 빌링키 record가 없으므로 예상된 terminal classification이다.
- 실제 빌링키는 코드·문서·테스트에 기록하지 않았다.

## 남은 작업

- 다음 세션의 최우선 작업으로 동일 `BILLING_DELETED` delivery를 한 번 더 재시도해
  transmission identity, HTTP 200, 55420 inbox dedupe row 수와 중복 상태 변경 여부를 확인
- 이어서 같은 admin DB의 정상 구독·카드 변경 lifecycle로 billing-key record를 만들고
  `BILLING_DELETED` current/candidate/previous fingerprint 분류와 terminal 전이를 확인
- raw key 직접 DB 삽입이나 기존 5433 데이터 수정은 사용하지 않으며, provider 조건 때문에
  만들 수 없는 slot은 통과/잔여 범위를 분리해 기록
- user-MID widget key 발급 뒤 `PAYMENT_STATUS_CHANGED`, `DEPOSIT_CALLBACK` 확인
- 라이브키·카드사 심사 뒤 실제 승인/청구 확인
- 선택적 실제 시간 기반 renewal/retry/past-due, 가상계좌, 연간 상품 수동 테스트
- feature push/PR/dev merge, DB backup/migration dry-run, 개발서버 배포
- 공유 DB에 `178665`가 이미 적용됐다면 수정 파일을 재실행하지 않고 새 forward
  compatibility migration 작성

상세 인계:
`../../../../main/2026-08-21-toss-payments-pg-upgrade-consent-and-manual-verification-handoff.md`

## BILLING_DELETED dedupe와 key-slot 후속 검증

- 새 세션 시작 시 일곱 feature worktree가 인계 HEAD에서 모두 clean임을 다시 확인했다.
  API는 `bf35efd`, 고객 웹은 `bb87a9a`였다.
- API 3000은 내려가 있었고 PostgreSQL 55420, 기존 5433, ngrok 4040은 살아 있었다.
  Node 22.22.3으로 API 3000과 고객 웹 4201을 다시 실행했다. API는 admin/payment DB만
  55420으로 override했고 기존 user/release DB를 유지했다. 5433에는 접속하거나 migration을
  실행하지 않았다.
- 성공 delivery에는 개발자센터 `다시 시도` 버튼이 없었다. Toss의 실패-only retry 정책과
  일치하므로 원 성공 기록은 유지하고 ngrok Agent API로 captured request를 한 번 exact
  replay했다.
- original/replay transmission ID, transmission time, retry count, body SHA-256, provider
  envelope identity가 전부 같았고 둘 다 API HTTP 200이었다. 55420 original dedupe row는
  한 건 그대로였고 row ID, fingerprint, terminal status/error/retry/timestamp가 변하지 않았다.
- 사용자가 같은 DB에서 Basic Monthly 테스트 결제를 완료했다. order
  `sub_b0a06d714cff47579625111fd87e2e3a`는 `paid`/fulfillment `succeeded`, active access와
  400-credit grant는 각각 한 건이었다.
- 사용자가 다른 카드로 정상 결제수단 변경을 완료했고 distinct fingerprint가 설치됐다.
  old key deletion은 개발자센터 성공(16:39:47 KST), ngrok/API HTTP 200,
  inbox `processed`/error 없음이었다.
- provider webhook은 previous cleanup이 `succeeded`로 pair를 지운 약 246ms 뒤 도착했다.
  exact previous slot 대신 completed attempt fingerprint의 installed/previous reclassification
  fallback으로 안전하게 종료됐다.
- 정상 lifecycle이 만든 installed current key는 one-use ignored probe가 read-only 조회,
  메모리 복호화, fingerprint 검증 뒤 Toss test DELETE했다. raw key/암호문은 출력·저장하지
  않았고 probe 파일은 즉시 제거했다. delivery는 개발자센터 성공(16:43:00 KST),
  ngrok/API HTTP 200, inbox `processed`/error 없음이었고 subscription fingerprint가 제거돼
  removal status `succeeded`가 됐다.
- 세 durable `BILLING_DELETED` row 모두 normalized payload에 raw `billingKey`나 provider
  `data` object를 저장하지 않았고 deposit ciphertext도 없었다.
- 사용자가 developer-center payload를 대화에 붙이면서 raw test key 값이 대화에는 노출됐다.
  두 값은 이미 성공적으로 삭제된 테스트 키이며 코드·문서·DB query output에는 재기록하지
  않았다.
- exact issued-candidate와 webhook 도착 시 extant-previous slot은 정상 provider/browser
  ordering으로 만들 수 없어 미검증으로 남겼다. raw key DB 삽입, 상태 조작, fault injection은
  사용하지 않았다.
- current delivery에서 `processed_at`이 DB `received_at`보다 약 28ms 빨랐다. claim 전에
  application time을 잡고 receipt는 DB time을 쓰는 clock-domain 경합이 root cause다.
  dedupe/terminal 분류에는 영향이 없었고 별도 timestamp-invariant bugfix 후보로 남겼다.
- API 체크리스트 갱신은 `d995a4d docs(payments): record billing deletion verification`으로
  커밋했고 API worktree를 clean 상태로 복구했다.

## 현재 상태

- 일곱 PG feature worktree clean
- API HEAD `b498b22004f3b1e3e838037fd1a02e8b558f516e`
- 고객 웹 HEAD `022f107185d6a5e0f9595bfa2876837bb338f25a`
- API 3000과 고객 웹 4201 실행 중
- webhook replay용 새 격리 PostgreSQL 55420 실행 중
- 55420의 active Basic Monthly subscription은 current-key 분류 검증 뒤 billing fingerprint가
  제거됐고 removal status가 `succeeded`다.
- 기존 5433 DB와 사용자 ngrok은 수정하지 않음

## Webhook timestamp invariant 수정과 전체 범위 재분류

- 사용자가 전체 PG 완료 판단은 사용자 권한임을 명확히 했고, 돈 환불/provider payment
  cancellation이 설계·구현되지 않은 상태를 전체 완료로 표현하지 않도록 문서를 정정했다.
- `PaymentWebhookService.processNext()`가 claim 전 한 번 잡은 application time을 success,
  manual review와 retry에 재사용해 DB `received_at`보다 terminal timestamp가 앞설 수 있었던
  root cause를 재현했다.
- TDD RED: claim 대기 중 28ms 뒤 수신된 행의 success `transitionedAt`이 과거 시각인 테스트와
  retry `transitionedAt`/`nextRetryAt`이 과거 기준인 테스트가 기존 코드에서 실패했다.
- 서비스는 테스트용 fixed time 계약을 유지하면서 운영 호출에서 claim과 terminal clock을
  각각 읽고 retry를 terminal transition 기준으로 계산하게 수정했다.
- repository claim SQL은 `processing_started_at`/`updated_at`을 `received_at`보다 빠르지 않게
  clamp하고, processed/manual-review/retry update는 receipt와 lease timestamp보다 빠른
  terminal/update 시각을 저장하지 않게 했다. schema migration은 필요하지 않았다.
- repository SQL 계약 4건도 기존 코드에서 RED를 확인했고 수정 후 focused 98/98, 전체 API
  154/154 suites와 1,330/1,330 tests, `npm run build`가 통과했다. sandbox에서는 예상대로
  localhost bind 2건만 `EPERM`이었고 동일 전체 명령을 sandbox 밖에서 재실행해 전부 통과했다.
- exact issued-candidate와 extant-previous 분류는 운영 cleanup 순서를 늦추거나 raw key를 직접
  INSERT해 수동 통과시키지 않는다. 실제 PostgreSQL repository와 제어 가능한 fake provider를
  사용하고 service/repository를 경유하는 통합 테스트를 후속 TODO로 남겼다.
- 전체 상태를 `구현 완료`, `검증 완료`, `구현 남음`, `검증 남음`으로 다시 분류했다. 원래
  승인된 무환불 Task 1~25 구현과 전체 제품 완료를 구분하며, 돈 환불은 전액/부분 정책,
  Toss 취소 idempotency, access/credit 영향, 권한/UI, 감사, webhook/reconciliation, 실패 복구
  설계부터 남아 있다.
- 기존 localhost:5433에는 접속하거나 migration을 실행하지 않았다. push/merge/PR/deploy도
  수행하지 않았다.
- API commit은 `1e90575 fix(payments): preserve webhook timestamp ordering`과
  `6585e3a docs(payments): close timestamp fix plan`이며 최종 HEAD는
  `6585e3ae1768f0ccbea94b0723b46e311966331b`다.
- 이후 가상계좌 due-date correction build로 API 3000을 다시 시작했다. 현재 PID `71764`,
  session `4592`, API/customer health HTTP 200이며 4201/4040/55420 listener를 유지한다.

## Toss Pay topup top-level redirect 검증

- PC Chrome 151에서 토스페이 휴대폰 인증 뒤 provider iframe이 top-level
  `localhost:4201`을 API callback `localhost:3000`으로 이동하려다 cross-origin navigation
  `SecurityError`로 차단되는 현상을 재현했다. report-only CSP 경고는 실제 차단 원인이 아니었다.
- 고객 웹 일회성 `widgets.requestPayment()`에 `windowTarget: 'self'`를 추가해 provider 인증과
  redirect를 최상위 창에서 수행하게 했다. 결제수단 선택 widget과 billing auth는 변경하지 않았다.
- TDD RED에서 exact SDK boundary test가 옵션 누락으로 실패했고, 수정 후 focused 3/3, 고객 웹
  전체 170/170 tests, production build가 통과했다. read-only review는 Critical/Important/Minor
  이슈 없이 commit 가능 판정이었다.
- 고객 웹 commit은 `022f107 fix(payments): open redirect checkout in top-level window`이다.
- 새 500-credit 토스페이 주문 `topup_3a04e581d598488b9f2cef14c53db54d`는 최상위 Toss sandbox
  화면에서 휴대폰 인증 뒤 local callback/result로 복귀했고 `paid`/`DONE`/fulfillment
  `succeeded`가 됐다. exactly-once DB 근거는 active 500-credit grant 1행, +500 ledger 1행,
  checkout-created/paid event 각 1행이다.
- 공용 `_docs` widget pair 결제이므로 연관 webhook inbox row는 0건이었다. 이 성공은 동기
  승인·지급 검증이며 user-MID `PAYMENT_STATUS_CHANGED` 검증은 여전히 남아 있다.
- API 체크리스트 갱신 commit은
  `b498b22 docs(payments): record Toss Pay topup verification`이다.
- 이전 iframe 차단 주문은 수동 승인하거나 재사용하지 않았다. 별도로 뒤이어 생성된
  1000-credit checkout은 마지막 관찰 시 `checkout_ready`/fulfillment `pending`이어서 성공
  범위에 포함하지 않았다.

## Remaining topup methods and virtual-account due-date correction

- 카카오페이 `topup_65cfabe45a1f4c2280e171ec1daea885`, 네이버페이
  `topup_e42f17e572424f4d832210ec1c888b89`, 퀵계좌이체
  `topup_f9d14df16ca3417a9bf5d89efa10354b`가 모두 `paid`/`DONE`/fulfillment
  `succeeded`로 완료됐다. 각 주문은 grant/ledger와 checkout-created/paid event가 정확히
  한 건이었다. 퀵계좌이체는 method `계좌이체`, easy-pay provider 없음으로 저장됐다.
- 주문서형 SDK는 유지했다. 문제는 SDK 방식이 아니라 API가 provider due date를 버리고
  `order.createdAt + 24h`를 저장하던 데 있었다.
- 최초 confirmation과 snapshot이 없는 first-observed reconciliation이 provider
  `virtualAccount.dueDate`를 공통 검증해 저장한다. offset ISO, 주문 생성 이후, 90일 이내가 아니면
  `TOPUP_VIRTUAL_ACCOUNT_RESPONSE_INVALID`로 fail closed한다.
- 실제 요청에 쓰이지 않던 `CheckoutSession.virtualAccountDueDate`를 API/OpenAPI/client
  model과 fixture에서 제거했다. migration과 provider/UI 변경은 없다.
- TDD RED에서 provider due date 불일치와 invalid date 수용을 재현했다. 수정 후 focused API
  126/126, 전체 API 154/154 suites·1,334/1,334 tests, 고객 웹 170/170, changed source
  ESLint와 양쪽 build가 통과했다.
- commit: API `b80b31e`, 고객 웹 `fd71462`.
- checklist scope clarification commit: API `a47a971`.
- 수정 전 방식의 실제 가상계좌 row와 배포 이력이 없어 legacy waiting/expired 호환 구현,
  backfill, migration은 범위에서 제외했다. 55420의 기존 수동 검증 데이터는 유지했다.
- 공용 `_docs` widget pair 때문에 가상계좌 노출/페이코 제거와 실제 issue/deposit/expiry는
  미실행이다. user-MID widget key/variant가 준비되면 결제 어드민 설정 후 검증한다.
- 돈 환불/provider cancellation은 설계·구현되지 않았다.

## 세션 종료와 다음 환불 설계 인계

- 다음 세션의 첫 목표를 돈 환불/provider payment cancellation **설계**로 확정했다. 구현,
  DB 변경, provider 취소 호출보다 과거 기록과 현재 구현의 read-only 대조를 먼저 수행한다.
- 환불 전용 완성 설계서는 찾지 못했다. 과거 논의는 다음 문서에 분산돼 있다.
  - `2026-08-19-clipper-product-policy-business-meeting.md` 7절
  - `2026-08-19-clipper-pg-product-policy-meeting-brief.md` 6절·10.10절
  - `2026-08-18-toss-payments-pg-integration-design.md` 14.3절·15절·22절
  - `2026-08-12-billing-product-catalog-foundation-plan.md` Phase 8
  - `2026-08-13-toss-pay-direct-to-toss-payments-pg-handoff.md` 후속 작업 메모
- 위 기록은 정답이 아니다. 다음 세션에서 전체 검색을 다시 수행하고 `현재도 유효`,
  `폐기/충돌`, `미결정`, `법률·운영 확인 필요`, `누락`으로 재분류한다.
- 다음 자동결제 취소, 실제 결제금 환불/provider 취소, 실패 작업의 내부 크레딧 반환·복구를
  서로 다른 lifecycle과 용어로 유지한다.
- Superpowers brainstorming으로 현황 요약 후 질문을 한 번에 하나씩 진행하고 2~3개 대안을
  비교한다. 사용자 설계 승인 전에는 구현하지 않는다.
- 종료 직전 API/고객 웹 worktree는 각각 `a47a971e744d07b5b3d0435ee4c17133e526a615`,
  `fd714624a80813b53fae2fdc4a155f6bf1250bbb`로 clean이었다. 3000·4201·55420 listener는
  관찰했으나 sandbox `curl` 연결은 불가했으므로 다음 세션에서 건강 상태를 재확인한다.
- 기존 5433에는 접속하거나 migration하지 않는다. merge/deploy/live charge/provider
  cancellation도 별도 사용자 승인 없이는 수행하지 않는다.
