# 2026-07-31 AI 숏폼 디렉터 부분 레퍼런스 성공 자동 진행

## 배경

선택한 레퍼런스 영상 3개 중 2개의 정밀 분석이 실제로 성공했지만, 기존 구현은 선택한
영상이 모두 성공해야만 조사 주제 생성으로 넘어갔다. 한 영상의 실패 때문에 이미 얻은
유효한 분석 결과도 사용하지 못하고 흐름이 멈추는 문제였다.

## 확정한 동작

- 정밀 분석이 하나 이상 성공하면 성공한 분석만 `reference-pattern-synthesis`와 주제 종합에
  전달하고 부모 조사를 자동으로 계속한다.
- child 정밀 분석 attempt의 실제 상태는 그대로 보존한다. 일부 실패가 있어 child가
  `partial`이어도 부모 조사 자체에 discovery 실패가 없으면 부모는 `succeeded`다.
- 성공 분석이 하나도 없으면 부모는 `awaiting_reference_selection`을 유지한다. 이 경우
  사용자는 실패 영상 재시도, 다른 영상 선택, 또는 정밀 분석 없이 계속(시장 근거만 사용)을
  선택할 수 있다.
- 모든 분석이 끝난 화면에는 `레퍼런스 N개 중 M개 분석 성공 · K개 실패`를 표시한다.
  성공이 0개면 가능한 세 경로를 설명한다.
- Angular는 부분 성공 attempt 뒤 부모의 종합이 완료될 때까지 폴링한다. 분석 목록 조회가
  일시적으로 실패해도 기존 상태와 승인 정보를 지우지 않고 재시도한다.

설계와 실행 계획:

- `.codex/design/SHORTFORM_DIRECTOR_PARTIAL_REFERENCE_CONTINUATION_DESIGN_2026-07-31.md`
- `.codex/design/SHORTFORM_DIRECTOR_PARTIAL_REFERENCE_CONTINUATION_EXECUTION_PLAN_2026-07-31.md`

## 구현 커밋

### Desktop Nest

- `89d280b` `fix(shortform-director): continue after partial reference success`

### Angular

- `b2aafd7` `fix(shortform-director): keep polling after partial reference success`
- `d531e33` `fix(shortform-director): retry partial analysis lookup`
- `0bba931` `feat(shortform-director): show partial reference analysis summary`
- `8d2d00d` `fix(shortform-director): hide empty reference summary`
- `1f88c6f` `fix(shortform-director): preserve partial reference results`
- `7aae602` `fix(shortform-director): bound reference reconciliation polling`
- `1f5e1f8` `fix(shortform-director): block duplicate reference starts`

## 2026-07-31 자동 검증

외부 provider/API 호출 없이 새로 실행했다.

- Desktop Nest: `npm run build` 통과.
- Desktop Nest: `node --test test/shortform-director-*.test.js` 결과 총 `691`, 통과 `689`, 실패
  `0`, skip `2`. skip은 기존 환경 의존 로컬 Motion Canvas H.264/AAC 렌더 테스트다.
- Angular: `./node_modules/.bin/ng test --watch=false` 결과 `1822/1822 SUCCESS`.
- Angular: `npm run build` 통과.
- `desktop/clipper_nestjs`, `desktop/clipper_angular`, `.codex`의 `git diff --check` 결과는 모두
  출력 없음이다.
- 검증 전후 코드 저장소는 clean이었으며, 이 기록 작업은 `.codex` 문서 파일만 변경한다.

## 최종 검토 후 보완

최종 검토에서 확인한 부분 성공 폴링의 경계 조건을 Angular에 보완했다. 외부 provider/API
호출은 하지 않았다.

- 부모 조사가 완료될 때 child attempt의 입력·진행·실패 artifact를 덮어쓰지 않고 유지한다.
  완료된 조사를 다시 선택해도 저장된 attempt와 분석 결과를 다시 읽어 카드와 요약을 복원한다.
- 분석 목록의 로컬 조회가 일시 실패한 시점에 부모가 완료돼도 조사 주제는 표시하되, 분석 목록이
  정상적으로 읽힐 때까지 폴링을 계속한다.
- 부모가 아직 종합 중이면 선택·건너뛰기·재분석으로 현재 폴링 세션을 무효화할 수 없도록
  레퍼런스 선택을 잠근다.
- 회귀 테스트 3개를 RED로 확인한 뒤 구현했다. 대상 스토어 `34/34` 통과, Angular 전체
  `1820/1820` 통과, `npm run build` 통과했다.

## 최종 검토 2차 보완

- child가 `partial`이 된 순간부터 부모 종합이 끝날 때까지 별도 reconciliation 상태를 유지해
  레퍼런스 선택 변경이 폴링을 취소하지 못하게 했다. 아직 분석을 시작하지 않은 정상 대기
  상태의 선택 편집은 그대로 가능하다.
- 부모 완료와 분석 목록 조회 실패가 겹치면 부모·attempt·topics는 한 번만 읽고, 분석 목록만
  최대 3회 별도 재시도한다. 성공 또는 한도 소진 시 잠금과 타이머를 해제하며 마지막 오류는
  한도 소진 때 그대로 남긴다.
- 회귀 테스트 3개를 RED로 확인한 뒤 대상 스토어 `36/36`, Angular 전체 `1822/1822`,
  `npm run build`를 통과했다. 외부 provider/API 호출은 없었다.

## 최종 검토 3차 보완

- reconciliation 중에는 기존 승인 preflight가 남아 있어도 스토어의 정밀 분석 재시작을
  차단했다. 같은 approval로 직접 재호출해도 gateway의 시작 호출은 최초 1회에서 늘지 않는다.
- 아이디어 페이지의 정밀 분석 승인 버튼도 reconciliation 상태를 `pending`으로 받아
  비활성화한다.
- RED 후 스토어·아이디어 페이지 집중 `52/52`, Angular 전체 `1822/1822`, `npm run build`
  통과했다. 외부 provider/API 호출은 없었다.

## 다음 실제 앱 재검증

1. 최신 Desktop Nest와 Angular/Electron 빌드로 앱을 실행한다.
2. 운영 프로필에서 조사 후 Shorts 2~5개를 선택해 레퍼런스 정밀 분석을 승인한다.
3. 일부 성공 사례에서 성공한 분석만으로 자동 종합되어 조사 주제가 표시되는지 확인한다.
   부모는 완료, child attempt는 부분 완료가 될 수 있고 성공/실패 요약은 보여야 한다.
4. 전부 실패 사례에서는 레퍼런스 선택 대기 상태에 머무르며 정밀 분석 없이 계속, 재시도,
   교체 중 하나를 선택할 수 있는지 확인한다.
5. 실제 외부 provider를 호출하기 전에는 앱의 preflight에 표시된 provider·모델·호출 수·예상
   비용을 확인하고, 사용자 화면 승인 뒤에만 실행한다.

## 다중 목적 운영 프로필의 YouTube 조사 범위 분리

### 문제

`자취방 꾸미기`와 `방 구하기`처럼 서로 다른 목적이 하나의 운영 프로필에 함께 있으면 기존
query-plan이 이를 `원룸 인테리어 수납 아이디어 자취방 집 구하기 팁` 같은 긴 검색어 하나로
합쳤다. 프로필에 여러 목적이 있는 것 자체는 정상이며, 유저에게 목적 하나만 입력하도록
강제해서는 안 된다. 조사 단계가 서로 다른 검색 의도를 분리해야 했다.

설계와 실행 계획:

- `.codex/design/SHORTFORM_DIRECTOR_MULTI_INTENT_RESEARCH_QUERY_DESIGN_2026-07-31.md`
- `.codex/design/SHORTFORM_DIRECTOR_MULTI_INTENT_RESEARCH_QUERY_IMPLEMENTATION_PLAN_2026-07-31.md`

### 구현

- Web API query-plan을 v3로 올리고 다음을 구조적으로 분리했다.
  - Naver 뉴스 검색어 1~2개
  - YouTube 조사 범위 1~2개
  - 각 YouTube 범위의 이름·실제 검색어·선정 이유
- 프롬프트가 운영 프로필의 다른 목적을 한 검색어에 이어 붙이지 않고 최대 2개의 독립된
  조사 범위로 나누게 했다. `focusKeyword`가 있으면 이를 최우선으로 사용한다.
- Desktop Nest는 각 범위에서 동일한 검색어로 조회수순 40개와 관련도순 40개를 모두
  요청한다. 최대 4회의 YouTube Search 호출, 최대 160개의 원시 결과를 허용한다.
- 첫 범위가 0건이어도 두 번째 범위를 계속 수집한다.
- 모든 고유 video ID를 Shorts URL로 검증한다. 같은 영상이 여러 범위에서 발견되면 검증은
  한 번만 하고 모든 수집 artifact 연결을 보존한다.
- Shorts 검증 artifact를 v2로 확장하되 v1 읽기 호환성과 기존 후보 snapshot 공개 계약은
  유지했다.
- 레퍼런스를 아직 선택하지 않은 기존 실행의 분석 목록은 정상적인 `[]`로 처리하고,
  존재하지 않는 실행만 404로 유지했다.
- Angular 화면은 다음을 사람이 읽는 형태로 표시한다.
  - 조사 범위 이름, 실제 검색어, 이 검색어를 고른 이유
  - 각 YouTube 호출의 조회수순/관련도순, 최대 40개 요청, 실제 결과 수
  - 후보별 `수집 근거 보기`의 실제 query, 정렬 기준, 수집 건수
  - 원본 JSON은 기존처럼 기본 접힘 상태로 별도 확인

구현 커밋:

- Web API: `29871e7` `feat(director): plan multi-intent research scopes`
- Desktop Nest:
  - `9bf6205` `feat(director): project multi-intent research scopes`
  - `d661edd` `feat(director): collect every research scope lane`
  - `d239795` `feat(director): preserve multi-scope Shorts evidence`
  - `a578dce` `fix(director): treat unselected references as empty`
- Angular: `c82489a` `feat(director): explain multi-scope research evidence`

### 검증

외부 provider/API 호출 없이 실행했다.

- Web API 전체: `120` suite, `1004` test 통과, `npm run build` 통과
- Desktop Nest AI 디렉터 전체: 총 `699`, 통과 `697`, 실패 `0`, skip `2`,
  `npm run build` 통과
- Angular 전체: `1825/1825 SUCCESS`, `npm run build` 통과
- 모든 구현 저장소의 `git diff --check` 결과 이상 없음
- Web API 전체 테스트를 Desktop Nest 테스트와 병렬로 처음 실행했을 때 구현과 무관한
  인증 선행검사 1개가 5초 timeout을 넘겼다. Web API만 단독으로 즉시 재실행해
  `1004/1004` 통과를 확인했으며 재현되지 않았다.
- 실제 YouTube·Naver·OpenAI·Gemini 호출은 하지 않았다. 새 계약의 실제 동작 확인은 최신
  앱에서 새 조사 사전 점검과 사용자 승인 후 진행한다.

### 저장소 상태 메모

- 구현 저장소 세 곳은 커밋 후 clean이다.
- `legacy/adlight_python/fastapi_server.spec`의 기존 Homebrew ffmpeg 경로 변경은 그대로
  보존했고 이번 작업에 포함하지 않았다.
- `clipper_docs`는 작업 트리는 clean이지만 확인 시점에 origin보다 1 commit behind였다.
  pull, reset, revert하지 않았다.
